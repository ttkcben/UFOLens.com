# GitHub — المنشور 3 من 3 · ملاحظات حول البنية (مناقشة بأسلوب ADR)

**يُستخدم كـ:** مناقشة تحت "عرض وإخبار" / "بنية"، أو بذرة لـ ADR في `docs/`.
**الكلمات المفتاحية:** بنية، ADR، آلة حالة تقدمية فقط، LLM محلي، Ollama، OCR، حوسبة الحافة، CSP، ترويسات الأمان، خط أنابيب البيانات، هندسة التكاليف، بيان SQLite، D1، R2، KV
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## لماذا تم بناء ufolens.com بالطريقة التي هو عليها

ملاحظات حول القرارات الثلاثة التي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث ومتعددة اللغات لـ [أرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات / الانتقادات.

### 1. خط الأنابيب هو آلة حالة تقدمية فقط — عن قصد

الحالات: `discovered → downloaded → ocr_done → translated → published`. تتحرك الوثيقة إلى الأمام فقط، وفقط عندما يكون هناك عمل للقيام به. المحتوى المنشور لا يعاد معالجته أبدًا ما لم ير كاشف الفروق أن المصدر قد تغير بالفعل.

**لماذا:** OCR + الترجمة هما العمليتان المكلفتان، والأرشيف ينمو بمرور الوقت. خط الأنابيب الذي "يعيد تشغيل كل شيء ليكون آمنًا" له تكلفة غير محدودة. جعل الانتقالات إلى الوراء مستحيلة يجعل الفاتورة الجامحة مستحيلة. سقف التكلفة هو خاصية من خصائص مخطط الحالة، وليس من يقظة المشغل.

**التكلفة:** ترحيل المخططات وإعادة المعالجة عن قصد أمران مزعجان بشكل متعمد. مقايضة مقبولة.

### 2. يعمل OCR والترجمة على LLM محلي، وليس عبر API سحابي

OCR: محرك مفتوح المصدر، مع Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على جهاز كمبيوتر محمول Apple Silicon.

**لماذا:** تكلفة هامشية صفرية لكل وثيقة؛ قابلية لإعادة الإنتاج (نموذج ثابت + مطالبات)؛ وخطوة الجلب يجب أن تعمل بالفعل من عنوان IP سكني (المصدر خلف Akamai Bot Manager — `curl` يحصل على 403)، لذا فإن الكمبيوتر المحمول موجود في الحلقة على أي حال.

**التكلفة:** جودة الترجمة أقل من نموذج متطور. بالنسبة لمجموعة مرجعية حيث يكون النص الإنجليزي الأصلي دائمًا على بعد نقرة واحدة، هذا جيد. نحن لا ندعي أن الترجمات موثوقة.

### 3. يتشارك النصفان واجهة واحدة بالضبط: حزمة منشورة

لا يكتب خط الأنابيب أبدًا إلى قاعدة بيانات الإنتاج مباشرة. إنه يصدر `{ SQL, بيان الأصول, قائمة مسح ذاكرة التخزين المؤقت }`. "النشر" = تطبيق تلك الحزمة (دفع SQL إلى قاعدة بيانات SQL على الحافة، مزامنة الأصول مع تخزين الكائنات، مسح مفاتيح ذاكرة التخزين المؤقت المسماة).

**لماذا:** يمكن للجانب المحلي والجانب الطرفي التطور بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" له نفس الشكل في كل مرة. العامل (Worker) هو تطبيق TypeScript/Hono صغير — CSP صارم (لا يوجد `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض `Accept-Language` + بلد→لغة، ذاكرة تخزين مؤقت للصفحات في KV لمدة 30 يومًا، ومهمة صيانة يومية مجدولة — ولا يحتاج أبدًا إلى معرفة كيفية إنشاء البيانات.

**التكلفة:** تغيير مخطط D1 يلمس ملفين (`pipeline/lib/manifest_schema.sql`، `db/schema.sql`). تأمين رخيص.

### أمور غير قابلة للتفاوض مدمجة في السلوك

- لا ينتمي إلى حكومة الولايات المتحدة؛ لا توجد شارات رسمية.
- يتم الحفاظ على التنقيحات الأصلية، ولا يتم عكسها أبدًا.
- الفيديو منسوب إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من قبل محركات البحث، مع إلغاء الاشتراك في كشط الذكاء الاصطناعي.

الرابط المباشر: https://www.ufolens.com · API: https://www.ufolens.com/api/v1
