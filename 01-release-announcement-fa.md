# GitHub — پست ۱ از ۳ · اعلامیه انتشار / بلوک README

**استفاده به عنوان:** بدنه یک GitHub Release، یک Discussion پین‌شده، یا بالای README مخزن.
**کلمات کلیدی:** UAP، UFO، آرشیو PURSUE، اسناد طبقه‌بندی‌نشده، داده‌های باز، جستجوی تمام متن، OCR، ترجمه ماشینی، LLM محلی، Ollama، محاسبات لبه، API عمومی، Hono، TypeScript، Python
**لینک‌ها:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — یک پلتفرم چندزبانه و قابل جستجو برای آرشیو PURSUE UAP

**پخش زنده:** https://www.ufolens.com  ·  **API:** https://www.ufolens.com/api/v1  ·  **آرشیو منبع:** https://www.war.gov/ufo

`ufolens.com` آرشیو **PURSUE** وزارت جنگ ایالات متحده از سوابق طبقه‌بندی‌نشده UAP / UFO را به عنوان یک پلتفرم دانش بازنشر می‌کند: جستجوی تمام متن، ترجمه ماشینی در سراسر مجموعه، کاوش در نقشه + خط زمانی، و یک API عمومی JSON. اسناد منبع، آثار دولت فدرال ایالات متحده هستند و در داخل ایالات متحده در مالکیت عمومی قرار دارند ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). این پروژه **وابسته به دولت ایالات متحده نیست**، از هیچ‌گونه نشان رسمی استفاده نمی‌کند و هرگز ویرایش‌های انجام شده را برنمی‌گرداند.

### معماری

```
ماشین محلی (Apple Silicon، IP مسکونی)        شبکه لبه
─────────────────────────────────────────           ─────────────────────────
pipeline/  (Python 3.10, هسته فقط با stdlib)           worker/  (TypeScript, Hono.js)
  fetch → OCR → translate → publish  (فقط رو به جلو)    /{lang}/...   صفحات
  OCR: موتور منبع باز (Tesseract CLI به عنوان جایگزین)     /api/v1/...   API عمومی
  translate / NER: LLM محلی (Gemma از طریق Ollama)        /admin        کنسول اپراتور
  state: مانیفست SQLite                             پشتیبانی شده توسط: پایگاه داده SQL لبه،
        │                                              ذخیره‌سازی اشیاء (PDFهای منبع)، کش KV
        └── یک بسته منتشر می‌کند: SQL + مانیفست دارایی + لیست پاک‌سازی کش ──┘
```

- **هزینه صفر برای هر سند در هوش مصنوعی ابری.** OCR و ترجمه به صورت محلی اجرا می‌شوند؛ ماشین وضعیت فقط رو به جلو (`کشف‌شده → دانلودشده → ocr_انجام‌شده → ترجمه‌شده → منتشرشده`) تضمین می‌کند که هیچ سندی مجدداً پردازش نمی‌شود مگر اینکه تغییر کرده باشد.
- **هسته پایپ‌لاین هیچ وابستگی شخص ثالثی ندارد** — ماژول‌های تجزیه / مانیفست / دلتا روی یک Python تمیز و بدون هیچ بسته pip نصب شده‌ای اجرا و تست می‌شوند؛ مراحل OCR/ترجمه در صورت عدم وجود بسته‌های اختیاری به آرامی تنزل می‌یابند.
- **سایت لبه** هدرهای امنیتی سختگیرانه + CSP (بدون `unsafe-inline`؛ JSON-LD درون خطی با sha256 پین شده است)، مذاکره زبان از طریق `Accept-Language` + نگاشت کشور، یک کش صفحه KV ۳۰ روزه، و یک کرون روزانه برای نگهداری را اعمال می‌کند.
- **به‌روزرسانی‌های افزایشی:** یک آشکارساز دلتا، شاخص منبع را مقایسه می‌کند و فقط تغییرات را به پایپ‌لاین بازمی‌گرداند.

### برای توسعه‌دهندگان

API عمومی در https://www.ufolens.com/api/v1 اسناد و فراداده‌ها را به صورت JSON برمی‌گرداند. دسترسی ناشناس دارای محدودیت نرخ است؛ برای سطوح پژوهشگر/توسعه‌دهنده، درخواست کلید کنید. برای نقاط پایانی و محدودیت‌ها، بخش API در سایت را ببینید.

### وضعیت

کد کامل است؛ سایت در https://www.ufolens.com مستقر شده است. پایگاه داده تولیدی با اجرای پایپ‌لاین آفلاین و انتشار بسته به جلو (`cli_publish run --remote`) پر می‌شود. اسناد طراحی کامل در `docs/20260511/` قرار دارند.

### مجوز / مرزها

- اسناد منبع: آثار دولت فدرال ایالات متحده، در مالکیت عمومی در داخل ایالات متحده.
- کد خود این پلتفرم: `LICENSE` را ببینید.
- سایت `Tdm-Reservation: 1` و `X-Robots-Tag: noai, noimageai` را ارسال می‌کند — توسط موتورهای جستجو قابل ایندکس است، اما از آموزش/خزش هوش مصنوعی انصراف داده است.
- فیلم‌های ویدیویی به DVIDS / AARO نسبت داده شده و این پروژه ادعایی بر آن‌ها ندارد.

از Issues و PRها استقبال می‌شود. لطفاً قبل از ایجاد تغییرات ساختاری، `CLAUDE.md` و `docs/20260511/00-*` را مطالعه کنید.

