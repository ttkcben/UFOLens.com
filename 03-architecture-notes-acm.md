# GitHub — منشور 3 من 3 · ملاحظات حول الهيكلية (مناقشة بأسلوب ADR)

**الاستخدام:** كمناقشة تحت "عرض ومشاركة" / "هيكلية"، أو كبذرة لملفات ADR في `docs/`.
**الكلمات المفتاحية:** architecture, ADR, forward-only state machine, local LLM, Ollama, OCR, edge computing, CSP, security headers, data pipeline, cost engineering, SQLite manifest, D1, R2, KV
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## لماذا تم بناء ufolens.com بهذه الطريقة

ملاحظات حول القرارات الثلاثة التي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث ومتعددة اللغات لـ[أرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات / الاعتراضات.

### 1. مسار المعالجة هو آلة حالة تقدمية فقط — عن قصد

الحالات: `مكتشف ← محمل ← تم التعرف الضوئي ← مترجم ← منشور`. تتحرك الوثيقة إلى الأمام فقط، وفقط عندما يكون هناك عمل للقيام به. لا تتم إعادة معالجة المحتوى المنشور أبدًا ما لم ير كاشف التغييرات أن المصدر قد تغير بالفعل.

**لماذا:** التعرف الضوئي على الحروف (OCR) والترجمة هما العمليتان المكلفتان، والأرشيف ينمو بمرور الوقت. مسار المعالجة الذي "يعيد تشغيل كل شيء ليكون في أمان" له تكلفة غير محدودة. جعل الانتقالات إلى الوراء مستحيلة يجعل الفاتورة الجامحة مستحيلة. سقف التكلفة هو خاصية من خصائص مخطط الحالة، وليس من يقظة المشغل.

**التكلفة:** هجرات المخطط وإعادة المعالجة عن قصد صعبة بشكل متعمد. مقايضة مقبولة.

### 2. يعمل التعرف الضوئي على الحروف (OCR) والترجمة على نموذج لغوي كبير (LLM) محلي، وليس عبر واجهة برمجة تطبيقات سحابية (API)

OCR: محرك مفتوح المصدر، مع Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على حاسوب محمول Apple Silicon.

**لماذا:** تكلفة هامشية صفرية لكل وثيقة؛ قابلة للتكرار (نموذج ثابت + أوامر)؛ وخطوة الجلب يجب أن تعمل بالفعل من عنوان IP سكني (المصدر محمي بواسطة Akamai Bot Manager — `curl` يحصل على 403)، لذلك فإن الحاسوب المحمول مشارك في الحلقة على أي حال.

**التكلفة:** جودة الترجمة أقل من نموذج متطور. بالنسبة لمجموعة مرجعية حيث الأصل الإنجليزي دائمًا على بعد نقرة واحدة، هذا جيد. نحن لا ندعي أن الترجمات موثوقة.

### 3. يتقاسم النصفان واجهة واحدة بالضبط: حزمة منشورة

لا يكتب مسار المعالجة أبداً في قاعدة بيانات الإنتاج مباشرة. إنه يصدر `{ SQL, بيان الأصول, قائمة إفراغ ذاكرة التخزين المؤقت }`. "النشر" = تطبيق تلك الحزمة إلى الأمام (دفع SQL إلى قاعدة بيانات SQL الحافة، مزامنة الأصول مع تخزين الكائنات، إفراغ مفاتيح ذاكرة التخزين المؤقت المحددة).

**لماذا:** يمكن للجانب المحلي والجانب الحافة التطور بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" له نفس الشكل في كل مرة. العامل (Worker) هو تطبيق TypeScript/Hono صغير — CSP صارم (بدون `unsafe-inline`؛ JSON-LD المضمن مثبت عبر sha256)، `Accept-Language` + التفاوض على اللغة حسب البلد، ذاكرة تخزين مؤقت KV للصفحات لمدة 30 يوماً، ومهمة صيانة يومية مجدولة — ولا يحتاج أبداً إلى معرفة كيفية صنع البيانات.

**التكلفة:** تغيير مخطط D1 يلمس ملفين (`pipeline/lib/manifest_schema.sql`، `db/schema.sql`). تأمين رخيص.

### أمور غير قابلة للتفاوض مدمجة في السلوك

- لا ينتمي إلى حكومة الولايات المتحدة؛ لا توجد شعارات رسمية.
- يتم الحفاظ على التنقيحات المصدرية، ولا يتم عكسها أبدًا.
- الفيديو منسوب إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من قبل محركات البحث، معطل عن الكشط بواسطة الذكاء الاصطناعي.

الرابط المباشر: https://www.ufolens.com · API: https://www.ufolens.com/api/v1
