# GitHub — المنشور 3 من 3 · ملاحظات حول البنية الهيكلية (مناقشة بأسلوب ADR)

**الاستخدام:** كمناقشة تحت قسم "Show and tell" / "Architecture"، أو كنواة لملف ADR في `docs/`.
**الكلمات المفتاحية:** architecture, ADR, forward-only state machine, local LLM, Ollama, OCR, edge computing, CSP, security headers, data pipeline, cost engineering, SQLite manifest, D1, R2, KV
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## ليش ufolens.com مبني بهالطريقة

ملاحظات حول القرارات الثلاثة اللي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث ومتعددة اللغات [لأرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات والنقد.

### 1. خط الأنابيب هو آلية حالة ذات مسار أمامي إجباري — عن قصد

الحالات: `discovered → downloaded → ocr_done → translated → published`. المستند يتحرك للأمام فقط، وفقط عندما يكون هناك عمل يجب القيام به. المحتوى المنشور لا يعاد معالجته أبدًا إلا إذا رأى كاشف التغييرات أن المصدر قد تغير بالفعل.

**السبب:** عمليات OCR + الترجمة هي العمليات المكلفة، والأرشيف ينمو مع مرور الوقت. خط الأنابيب الذي "يعيد تشغيل كل شيء ليكون في الجانب الآمن" تكلفته غير محدودة. جعل الانتقالات الخلفية مستحيلة يجعل الفواتير المنفلتة مستحيلة. سقف التكلفة هو خاصية من خصائص مخطط الحالة، وليس نتاجًا ليقظة المشغل.

**المقابل:** ترحيل المخططات وإعادة المعالجة عن قصد هي عمليات صعبة بشكل متعمد. وهذا مقايضة مقبولة.

### 2. عمليات OCR والترجمة تعمل على LLM محلي، وليس عبر API سحابي

OCR: محرك مفتوح المصدر، مع Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على جهاز لابتوب Apple Silicon.

**السبب:** تكلفة هامشية صفر لكل مستند؛ نتائج قابلة للتكرار (نموذج ومطالبات ثابتة)؛ وخطوة الجلب تحتاج بالفعل إلى العمل من IP منزلي (لأن المصدر محمي بـ Akamai Bot Manager — استخدام `curl` يعطي خطأ 403)، لذلك فإن اللابتوب موجود في الدورة على أي حال.

**المقابل:** جودة الترجمة أقل من النماذج الرائدة. بالنسبة لمجموعة مرجعية حيث الأصل الإنجليزي دائمًا على بعد نقرة واحدة، هذا مقبول. نحن لا ندعي أن الترجمات موثوقة.

### 3. النصفان يتشاركان واجهة واحدة بالضبط: حزمة منشورة

خط الأنابيب لا يكتب أبدًا في قاعدة البيانات الإنتاجية مباشرة. بل يُصدر `{ SQL, بيان الأصول, قائمة إفراغ ذاكرة التخزين المؤقت }`. "النشر" = تطبيق هذه الحزمة (دفع SQL إلى قاعدة بيانات SQL الطرفية، مزامنة الأصول مع تخزين الكائنات، إفراغ مفاتيح ذاكرة التخزين المؤقت المحددة).

**السبب:** يمكن للجانب المحلي والجانب الطرفي التطور بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" له نفس الشكل في كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — CSP صارم (بدون `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض `Accept-Language` + بلد→لغة، ذاكرة تخزين مؤقت للصفحات (KV cache) لمدة 30 يومًا، مهمة صيانة يومية مجدولة — ولا يحتاج أبدًا إلى معرفة كيف تم إنشاء البيانات.

**المقابل:** تغيير مخطط D1 يتطلب تعديل ملفين (`pipeline/lib/manifest_schema.sql`، `db/schema.sql`). تأمين رخيص.

### مبادئ غير قابلة للتفاوض مدمجة في السلوك

- لا يتبع للحكومة الأمريكية؛ لا توجد شعارات رسمية.
- يتم الحفاظ على التنقيحات في المصدر، ولا يتم عكسها أبدًا.
- الفيديو منسوب إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من قبل محركات البحث، مع إلغاء الاشتراك من كشط الذكاء الاصطناعي.

الموقع المباشر: https://www.ufolens.com · API: https://www.ufolens.com/api/v1
