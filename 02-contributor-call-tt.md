# GitHub — 3 язманың 2 нчесе · Катнашучыларны чакыру / "яхшы беренче мәсьәләләр"

**Куллану:** Беркетелгән Discussion ("Contributing & good first issues") яки CONTRIBUTING.md кереш өлеше.
**Ачкыч сүзләр:** open source, contributing, good first issue, i18n, localization, OCR, Python, TypeScript, Vitest, pytest, accessibility, UAP, open data
**Гиперсылтамалар:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## ufolens.com'га өлеш кертү

[ufolens.com](https://www.ufolens.com) АКШ Хәрби департаментының [PURSUE UAP архивын](https://www.war.gov/ufo) эзләнә торган, күп телле платформага, [ачык API](https://www.ufolens.com/api/v1) белән әйләндерә. Ул ике өлештән тора — җирле Python кабул итү конвейеры (`pipeline/`) һәм TypeScript/Hono edge-кушымтасы (`worker/`) — бер интерфейста очраша: бастырылган SQL + активлар пакеты.

Өлеш кертү өчен сезгә болытлы хезмәтләр өчен керү мәгълүматлары кирәк түгел. Конвейерның төп модульләре stdlib-only һәм Worker тестлары хәтердәге саклагычка каршы эшли.

### Көйләү

```bash
# pipeline
python3 -m pytest pipeline/tests/          # should be all green, no pip install needed

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### Ярдәм иң кирәкле урыннар

**i18n / локализация** — `worker/src/i18n/ui-strings.json` — интерфейс юлларының чыганагы. Инглиз теленнән башка телләрне туган теле буларак белүчеләрнең тикшерүе бик кыйммәтле: уңайсыз машина тәрҗемәсен табу, RTL/макет проблемаларын төзәтү, телне билгеләү очракларын яхшырту.

**OCR сыйфаты** — OCR алдыннан иске басма машинкада язылган сканнарны яхшырак алдан эшкәртү; ачык чыганаклы движокны Tesseract'ка каршы үрнәк битләрдә чагыштыру өчен бәяләү системасы.

**Керемлелек** — `worker/src/render/` папкасындагы битләрне WCAG'га каршы аудит ясау; CSP катгый (`unsafe-inline` юк), шуңа күрә чишелешләр шул кысаларда эшләргә тиеш.

**API эргономикасы** — `worker/src/routes/` — битләргә бүлү, фильтрлау, OpenAPI тасвирламасы, үрнәк клиентлар.

**Конвейер ышанычлылыгы** — функциональлекне саклау юлларын арттыру, прогресс турында яхшырак отчет бирү, дельта-ачыклау очраклары (`pipeline/lib/delta.py`).

**Документация** — `docs/20260511/` (繁體中文; `00-*` — индекс). Дизайн документларын инглиз теленә тәрҗемә итү хуплана.

### Төп кагыйдәләр

- Барлык юллар да чагыштырмача — проект төрле машиналарда күчереп йөртелерлек булырга тиеш. Катгый абсолют юллар юк.
- Конвейерның *төп* модуленә pip бәйлелеген өстәмәгез. Өстәмә этаплар өстәмә пакетларны куллана ала, әмма аларсыз да функциональлекне сакларга тиеш.
- Алга гына хәрәкәт итүче халәт машинасын зәгыйфьләтмәгез — бу чыгымнарны чикләү ысулы.
- АКШ хөкүмәтенең рәсми билгеләрен өстәмәгез һәм чыганактагы редакцияләрне кире кайтара торган бернәрсә дә өстәмәгез.
- D1 схемасы үзгәрешләре **ике** файлга кагыла: `pipeline/lib/manifest_schema.sql` һәм `db/schema.sql`.
- Яңа код белән тестлар. Conventional-commit хәбәрләре.

Башта `CLAUDE.md` һәм `docs/20260511/00-*` укыгыз, аннары PR ясаганчы теләсә нинди структур үзгәрешне тикшерү өчен мәсьәлә ачыгыз.
