# GitHub — منشور 3 من 3 · ملاحظات حول الهيكلية (مناقشة بأسلوب ADR)

**يُستخدم كـ:** مناقشة تحت "عرض ومشاركة" / "هيكلية"، أو كنواة لـ ADR في `docs/`.
**كلمات مفتاحية:** هيكلية, ADR, آلة حالات ذات اتجاه أمامي فقط, LLM محلي, Ollama, OCR, حوسبة طرفية, CSP, ترويسات أمان, data pipeline, هندسة التكاليف, بيان SQLite, D1, R2, KV
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## ليش ufolens.com مبني بهالطريقة

ملاحظات حول القرارات الثلاثة التي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث والمتعددة اللغات لـ [أرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات والانتقادات البناءة.

### 1. الـ pipeline هو آلة حالات ذات اتجاه أمامي فقط — عن قصد

الحالات: `مكتشَف ← محمَّل ← تم_OCR ← مترجَم ← منشوُر`. المستند يتحرك للأمام فقط، وفقط عندما يكون هناك عمل للقيام به. المحتوى المنشور لا يعاد معالجته أبدًا إلا إذا رأى كاشف التغييرات أن المصدر قد تغير بالفعل.

**ليش:** عمليات OCR + الترجمة هي العمليات المكلفة، والأرشيف ينمو مع مرور الوقت. الـ pipeline الذي "يعيد تشغيل كل شيء للأمان" لديه تكلفة غير محدودة. جعل الانتقالات إلى الوراء مستحيلة يجعل الفاتورة الهاربة مستحيلة. سقف التكلفة هو خاصية من خصائص مخطط الحالات، وليس من انتباه المشغل.

**التكلفة:** ترحيل المخططات وإعادة المعالجة عن قصد هي عمليات صعبة بشكل متعمد. مقايضة مقبولة.

### 2. عمليات OCR والترجمة تعمل على LLM محلي، وليس على API سحابي

OCR: محرك مفتوح المصدر، Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على لابتوب Apple Silicon.

**ليش:** تكلفة هامشية صفرية لكل مستند؛ قابلية للتكرار (نموذج ثابت + prompts)؛ وخطوة الجلب يجب أن تعمل بالفعل من IP سكني (المصدر خلف Akamai Bot Manager — `curl` يحصل على 403)، لذلك فإن اللابتوب موجود في الحلقة على أي حال.

**التكلفة:** جودة الترجمة أقل من النماذج الرائدة. بالنسبة لمجموعة مرجعية حيث الأصل الإنجليزي دائمًا على بعد نقرة واحدة، هذا مقبول. نحن لا ندعي أن الترجمات موثوقة.

### 3. يتقاسم النصفان واجهة واحدة بالضبط: حزمة منشورة

لا يكتب الـ pipeline أبدًا في قاعدة بيانات الإنتاج مباشرة. بل يصدر `{ SQL، بيان الأصول، قائمة مسح ذاكرة التخزين المؤقت }`. "النشر" = تطبيق تلك الحزمة إلى الأمام (دفع SQL إلى قاعدة بيانات SQL الطرفية، مزامنة الأصول مع تخزين الكائنات، مسح مفاتيح ذاكرة التخزين المؤقت المحددة).

**ليش:** يمكن للجانب المحلي والجانب الطرفي أن يتطورا بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" له نفس الشكل في كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — CSP صارم (بدون `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض `Accept-Language` + الدولة←اللغة، ذاكرة تخزين مؤقت للصفحات في KV لمدة 30 يومًا، مهمة صيانة يومية مجدولة — ولا يحتاج أبدًا إلى معرفة كيف تم صنع البيانات.

**التكلفة:** تغيير مخطط D1 يلمس ملفين (`pipeline/lib/manifest_schema.sql`، `db/schema.sql`). تأمين رخيص.

### أمور غير قابلة للتفاوض مدمجة في السلوك

- غير تابع لحكومة الولايات المتحدة؛ لا شعارات رسمية.
- يتم الحفاظ على تنقيحات المصدر، ولا يتم عكسها أبدًا.
- الفيديو منسوب إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من قبل محركات البحث، مع إلغاء الاشتراك في كشط الذكاء الاصطناعي.

الموقع المباشر: https://www.ufolens.com · API: https://www.ufolens.com/api/v1

