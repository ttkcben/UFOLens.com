# GitHub — المنشور 1 من 3 · إعلان الإصدار / بلوك README

**يُستعمل كـ:** نص أساسي لإصدار GitHub، مناقشة مثبتة، أو في أعلى ملف README الخاص بالمستودع.
**كلمات مفتاحية:** UAP، UFO، أرشيف PURSUE، وثائق رفعت عنها السرية، بيانات مفتوحة، بحث في النص الكامل، OCR، ترجمة آلية، LLM محلي، Ollama، حوسبة الحافة، API عام، Hono، TypeScript، Python
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — منصة متعددة اللغات وقابلة للبحث لأرشيف PURSUE UAP

**الموقع المباشر:** https://www.ufolens.com · **الـ API:** https://www.ufolens.com/api/v1 · **الأرشيف المصدر:** https://www.war.gov/ufo

`ufolens.com` يعيد نشر أرشيف **PURSUE** التابع لوزارة الحرب الأمريكية والذي يحتوي على سجلات UAP / UFO التي رفعت عنها السرية، كمنصة معرفية: بحث في النص الكامل، ترجمة آلية عبر المجموعة الكاملة، استكشاف عبر الخريطة والخط الزمني، وواجهة برمجة تطبيقات JSON عامة. الوثائق المصدر هي من أعمال الحكومة الفيدرالية الأمريكية وتدخل ضمن الملك العام داخل الولايات المتحدة ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). هذا المشروع **ليس له أي انتماء للحكومة الأمريكية**، ولا يستخدم أي شارات رسمية، ولا يقوم أبداً بعكس عمليات التنقيح.

### الهيكلية

```
Local machine (Apple Silicon, residential IP)        Edge network
─────────────────────────────────────────           ─────────────────────────
pipeline/  (Python 3.10, stdlib-only core)           worker/  (TypeScript, Hono.js)
  fetch → OCR → translate → publish  (forward-only)    /{lang}/...   pages
  OCR: open-source engine (Tesseract CLI fallback)     /api/v1/...   public API
  translate / NER: local LLM (Gemma via Ollama)        /admin        operator console
  state: SQLite manifest                             backed by: edge SQL DB, object
        │                                              storage (source PDFs), KV cache
        └── publishes a bundle: SQL + asset manifest + cache-purge list ──┘
```

- **تكلفة صفرية لكل وثيقة على الذكاء الاصطناعي السحابي.** تتم عمليات الـ OCR والترجمة محلياً؛ وتضمن آلة الحالة ذات الاتجاه الواحد (`مكتشف ← محمل ← تم_التعرف_الضوئي ← مترجم ← منشور`) عدم إعادة معالجة أي وثيقة ما لم تتغير.
- **جوهر الـ pipeline ليس له أي تبعيات من أطراف ثالثة** — وحدات التحليل / البيان / الدلتا تعمل وتُختبر على Python نظيف بدون أي تثبيت من pip؛ وتتدهور مراحل OCR/الترجمة برشاقة عند غياب الحزم الاختيارية.
- **موقع الحافة (Edge site)** يطبق ترويسات أمان صارمة + CSP (بدون `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، والتفاوض على اللغة عبر `Accept-Language` + ربط البلدان، وذاكرة تخزين مؤقت للصفحات في KV لمدة 30 يوماً، ومهمة صيانة يومية مجدولة (cron).
- **تحديثات تدريجية:** كاشف دلتا (delta detector) يقارن فهرس المصدر ويغذي فقط التغييرات مرة أخرى في الـ pipeline.

### للمطورين

الـ API العام على https://www.ufolens.com/api/v1 يرجع الوثائق والبيانات الوصفية بصيغة JSON. الوصول المجهول محدود المعدل؛ اطلب مفتاحًا لفئات الباحثين/المطورين. انظر قسم الـ API على الموقع للاطلاع على نقاط النهاية والحدود.

### الحالة

الكود مكتمل؛ الموقع منشور على https://www.ufolens.com. يتم ملء قاعدة البيانات الإنتاجية عن طريق تشغيل الـ pipeline دون اتصال ونشر الحزمة إلى الأمام (`cli_publish run --remote`). توجد وثائق التصميم الكاملة في `docs/20260511/`.

### الترخيص / الحدود

- الوثائق المصدر: أعمال الحكومة الفيدرالية الأمريكية، ملك عام داخل الولايات المتحدة.
- الكود الخاص بهذه المنصة: انظر `LICENSE`.
- يرسل الموقع `Tdm-Reservation: 1` و `X-Robots-Tag: noai, noimageai` — قابل للفهرسة من قبل محركات البحث، مع إلغاء الاشتراك في تدريب/كشط الذكاء الاصطناعي.
- لقطات الفيديو منسوبة إلى DVIDS / AARO ولا يطالب هذا المشروع بملكيتها.

نرحب بالمشاكل (Issues) وطلبات السحب (PRs). يرجى قراءة `CLAUDE.md` و `docs/20260511/00-*` قبل فتح أي تغييرات هيكلية.

