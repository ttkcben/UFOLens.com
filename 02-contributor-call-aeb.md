# GitHub — المنشور 2 من 3 · دعوة للمساهمة / "مشاكل أولى جيدة"

**يُستعمل كـ:** مناقشة مثبتة ("المساهمة ومشاكل أولى جيدة") أو مقدمة لـ CONTRIBUTING.md.
**كلمات مفتاحية:** مفتوح المصدر، مساهمة، مشكلة أولى جيدة، i18n، توطين، OCR، Python، TypeScript، Vitest، pytest، وصولية، UAP، بيانات مفتوحة
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة في ufolens.com

[ufolens.com](https://www.ufolens.com) يحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) التابع لوزارة الحرب الأمريكية إلى منصة قابلة للبحث ومتعددة اللغات مع [API عام](https://www.ufolens.com/api/v1). يتكون المشروع من نصفين — pipeline استيعاب محلي بلغة Python (`pipeline/`) وتطبيق حافة (edge app) بـ TypeScript/Hono (`worker/`) — يلتقيان عند واجهة واحدة: حزمة SQL + أصول منشورة.

لا تحتاج إلى أي بيانات اعتماد سحابية للمساهمة. الوحدات الأساسية للـ pipeline هي stdlib-only فقط واختبارات الـ Worker تعمل مقابل تخزين في الذاكرة.

### الإعداد

```bash
# pipeline
python3 -m pytest pipeline/tests/          # should be all green, no pip install needed

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### أين تكون المساعدة أكثر فائدة

**i18n / التوطين** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة أي لغة غير إنجليزية من قبل متحدث أصلي لها قيمة عالية: اكتشاف المخرجات الآلية غير الملائمة، إصلاح مشاكل RTL/التخطيط، تحسين الحالات الهامشية للتفاوض على اللغة.

**جودة الـ OCR** — معالجة مسبقة أفضل للمسح الضوئي القديم المطبوع بالآلة الكاتبة قبل الـ OCR؛ أداة تقييم تقارن بين المحرك مفتوح المصدر والبديل Tesseract على صفحات عينة.

**الوصولية (Accessibility)** — تدقيق الصفحات المعروضة (`worker/src/render/`) مقابل WCAG؛ الـ CSP صارم (لا يوجد `unsafe-inline`)، لذلك يجب أن تعمل الحلول ضمن هذا الإطار.

**سهولة استخدام الـ API** — `worker/src/routes/` — التقسيم إلى صفحات (pagination)، التصفية (filtering)، وصف OpenAPI، أمثلة للعملاء.

**متانة الـ Pipeline** — مسارات تدهور أكثر رشاقة، تقارير تقدم أفضل، حالات هامشية في كشف الدلتا (`pipeline/lib/delta.py`).

**الوثائق** — `docs/20260511/` (繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة وثائق التصميم إلى اللغة الإنجليزية.

### القواعد الأساسية

- كل المسارات نسبية — يجب أن يكون المشروع قابلاً للنقل بين الأجهزة. لا توجد مسارات مطلقة مبرمجة بشكل ثابت.
- لا تضف تبعية pip إلى وحدة *أساسية* في الـ pipeline. قد تستخدم المراحل الاختيارية حزماً اختيارية، ويجب أن تتدهور برشاقة بدونها.
- لا تضعف آلة الحالة ذات الاتجاه الواحد — هذا هو سقف التكلفة.
- لا تدخل شارات رسمية للحكومة الأمريكية، ولا تضف أي شيء يعكس عمليات التنقيح في المصدر.
- تغييرات مخطط D1 تلمس ملفين **اثنين**: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- الاختبارات مع الكود الجديد. رسائل commit تقليدية (Conventional-commit).

اقرأ `CLAUDE.md` و `docs/20260511/00-*` أولاً، ثم افتح مشكلة (issue) لمناقشة أي شيء هيكلي قبل طلب السحب (PR).

