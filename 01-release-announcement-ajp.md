# GitHub — منشور 1 من 3 · إعلان الإصدار / قسم README

**يُستخدم كـ:** نص أساسي لإصدار على GitHub، أو مناقشة مثبتة، أو في أعلى README للمستودع.
**كلمات مفتاحية:** UAP, UFO, أرشيف PURSUE, وثائق رُفعت عنها السرية, بيانات مفتوحة, بحث في النص الكامل, OCR, ترجمة آلية, LLM محلي, Ollama, حوسبة طرفية, API عام, Hono, TypeScript, Python
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — منصة متعددة اللغات وقابلة للبحث لأرشيف PURSUE UAP

**الموقع المباشر:** https://www.ufolens.com  ·  **API:** https://www.ufolens.com/api/v1  ·  **الأرشيف المصدر:** https://www.war.gov/ufo

`ufolens.com` يعيد نشر أرشيف **PURSUE** التابع لوزارة الحرب الأمريكية، والذي يحتوي على سجلات UAP / UFO رُفعت عنها السرية، كمنصة معرفية: بحث في النص الكامل، ترجمة آلية عبر كامل مجموعة المستندات، استكشاف الخرائط والخطوط الزمنية، وواجهة API عامة بصيغة JSON. الوثائق المصدر هي من أعمال الحكومة الفيدرالية للولايات المتحدة وتقع ضمن الملكية العامة داخل الولايات المتحدة ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). هذا المشروع **غير تابع لحكومة الولايات المتحدة**، ولا يستخدم أي شعارات رسمية، ولا يقوم مطلقًا بإلغاء التنقيحات.

### الهيكلية

```
الجهاز المحلي (Apple Silicon، IP سكني)             الشبكة الطرفية (Edge network)
─────────────────────────────────────────           ─────────────────────────
pipeline/ (Python 3.10، الوحدات الأساسية لا تتطلب مكتبات خارجية) worker/ (TypeScript, Hono.js)
  جلب ← OCR ← ترجمة ← نشر (اتجاه أمامي فقط)             /{lang}/...   صفحات
  OCR: محرك مفتوح المصدر (Tesseract CLI كبديل)     /api/v1/...   API عام
  ترجمة / NER: LLM محلي (Gemma عبر Ollama)          /admin        لوحة تحكم المشغل
  الحالة: بيان SQLite                             مدعوم بـ: قاعدة بيانات SQL طرفية، تخزين
        │                                              كائنات (ملفات PDF المصدر)، ذاكرة تخزين مؤقت KV
        └── ينشر حزمة: SQL + بيان الأصول + قائمة لمسح ذاكرة التخزين المؤقت ──┘
```

- **تكلفة صفرية لكل مستند على سحابة الذكاء الاصطناعي.** تتم عمليات OCR والترجمة محليًا؛ آلة الحالات ذات الاتجاه الأمامي فقط (`مكتشَف ← محمَّل ← تم_OCR ← مترجَم ← منشوُر`) تضمن عدم إعادة معالجة أي مستند إلا إذا تغير.
- **الوحدات الأساسية للـ pipeline ليس لها أي تبعيات على أطراف ثالثة** — وحدات التحليل / البيان / التغييرات تعمل وتُختبر على بيئة Python نظيفة بدون أي تثبيت عبر pip؛ مراحل OCR/الترجمة تعمل بشكل أقل كفاءة ولكن لا تتوقف عند غياب الحزم الاختيارية.
- **الموقع الطرفي** يطبق ترويسات أمان صارمة + CSP (بدون `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض اللغة عبر `Accept-Language` + ربط الدولة، ذاكرة تخزين مؤقت للصفحات في KV لمدة 30 يومًا، ومهمة صيانة يومية مجدولة.
- **تحديثات تدريجية:** كاشف التغييرات يقارن فهرس المصدر ويغذي الـ pipeline بالتغييرات فقط.

### للمطورين

الـ API العام على https://www.ufolens.com/api/v1 يعرض المستندات والبيانات الوصفية بصيغة JSON. الوصول المجهول محدود المعدل؛ اطلب مفتاحًا للوصول إلى فئات الباحثين/المطورين. انظر قسم الـ API على الموقع للاطلاع على نقاط النهاية والحدود.

### الحالة

الكود مكتمل؛ الموقع منشور على https://www.ufolens.com. يتم ملء قاعدة بيانات الإنتاج عن طريق تشغيل الـ pipeline دون اتصال بالإنترنت ونشر الحزمة (`cli_publish run --remote`). وثائق التصميم الكاملة موجودة في `docs/20260511/`.

### الترخيص / الحدود

- الوثائق المصدر: من أعمال الحكومة الفيدرالية الأمريكية، وهي ملكية عامة داخل الولايات المتحدة.
- الكود الخاص بهذه المنصة: انظر `LICENSE`.
- يرسل الموقع `Tdm-Reservation: 1` و `X-Robots-Tag: noai, noimageai` — مما يجعله قابلًا للفهرسة من قبل محركات البحث، ولكنه يمنع استخدامه في تدريب/كشط الذكاء الاصطناعي.
- لقطات الفيديو منسوبة إلى DVIDS / AARO ولا يدعي هذا المشروع ملكيتها.

نرحب بالمشاكل وطلبات السحب (PRs). يرجى قراءة `CLAUDE.md` و `docs/20260511/00-*` قبل فتح تغييرات هيكلية.

