# GitHub — โพสต์ 3 จาก 3 · บันทึกสถาปัตยกรรม (Discussion สไตล์ ADR)

**ใช้เป็น:** Discussion ใต้หัวข้อ "Show and tell" / "Architecture" หรือเป็นต้นแบบ ADR ของ `docs/`
**คำสำคัญ:** สถาปัตยกรรม, ADR, เครื่องสถานะที่เดินหน้าเท่านั้น, LLM ท้องถิ่น, Ollama, OCR, edge computing, CSP, security headers, data pipeline, cost engineering, SQLite manifest, D1, R2, KV
**ไฮเปอร์ลิงก์:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## เหตุผลที่ ufolens.com ถูกสร้างขึ้นมาในรูปแบบนี้

บันทึกเกี่ยวกับการตัดสินใจสามประการที่กำหนดรูปแบบของ [ufolens.com](https://www.ufolens.com) (การสร้างใหม่ของ [PURSUE UAP archive](https://www.war.gov/ufo) ที่ค้นหาได้และหลายภาษา) ยินดีรับความคิดเห็น / ข้อโต้แย้ง

### 1. ไปป์ไลน์เป็นเครื่องสถานะที่เดินหน้าเท่านั้น — โดยตั้งใจ

สถานะ: `discovered → downloaded → ocr_done → translated → published` เอกสารจะเคลื่อนไปข้างหน้าเท่านั้น และเฉพาะเมื่อมีงานที่ต้องทำ เนื้อหาที่เผยแพร่แล้วจะไม่ถูกประมวลผลซ้ำเว้นแต่ตัวตรวจจับ delta จะเห็นว่าแหล่งที่มามีการเปลี่ยนแปลงจริง

**เหตุผล:** OCR + การแปลเป็นการดำเนินงานที่มีค่าใช้จ่ายสูง และคลังข้อมูลจะเติบโตขึ้นเรื่อย ๆ ไปป์ไลน์ที่ "รันทุกอย่างใหม่เพื่อความปลอดภัย" มีค่าใช้จ่ายที่ไม่จำกัด การทำให้การเปลี่ยนสถานะย้อนกลับเป็นไปไม่ได้ทำให้ค่าใช้จ่ายที่ควบคุมไม่ได้เป็นไปไม่ได้ เพดานค่าใช้จ่ายเป็นคุณสมบัติของกราฟสถานะ ไม่ใช่ความระมัดระวังของผู้ดูแล

**ต้นทุน:** การย้ายสกีมาและการประมวลผลซ้ำโดยตั้งใจนั้นยุ่งยากอย่างจงใจ เป็นการแลกเปลี่ยนที่ยอมรับได้

### 2. OCR และการแปลทำงานบน LLM ท้องถิ่น ไม่ใช่ API บนคลาวด์

OCR: เอ็นจิ้นโอเพนซอร์ส, Tesseract CLI fallback การแปล + NER: Gemma ผ่าน Ollama บนแล็ปท็อป Apple Silicon

**เหตุผล:** ไม่มีต้นทุนส่วนเพิ่มต่อเอกสาร; สามารถทำซ้ำได้ (โมเดลและพรอมต์คงที่); และขั้นตอนการดึงข้อมูลต้องทำงานจาก IP ที่อยู่อาศัยอยู่แล้ว (แหล่งที่มาอยู่หลัง Akamai Bot Manager — `curl` จะได้รับ 403) ดังนั้นจึงมีแล็ปท็อปอยู่ในวงจรอยู่แล้ว

**ต้นทุน:** คุณภาพการแปลต่ำกว่าโมเดลระดับแนวหน้า สำหรับคลังข้อมูลอ้างอิงที่ต้นฉบับภาษาอังกฤษอยู่ห่างออกไปเพียงคลิกเดียว นั่นก็ใช้ได้ เราไม่ได้อ้างว่าการแปลนั้นมีความน่าเชื่อถือ

### 3. สองส่วนแบ่งปันอินเทอร์เฟซเดียวเท่านั้น: bundle ที่เผยแพร่

ไปป์ไลน์ไม่เคยเขียนไปยังฐานข้อมูลการผลิตโดยตรง มันสร้าง `{ SQL, asset manifest, cache-purge list }` "การเผยแพร่" = นำ bundle นั้นไปใช้ (พุช SQL ไปยัง edge SQL DB, ซิงค์สินทรัพย์ไปยัง object storage, ล้างคีย์แคชที่มีชื่อ)

**เหตุผล:** ฝั่งท้องถิ่นและฝั่ง edge สามารถพัฒนาได้อย่างอิสระ; bundle สามารถตรวจสอบได้; และ "การปรับใช้ข้อมูล" มีรูปแบบเดียวกันทุกครั้ง Worker เป็นแอป TypeScript/Hono ขนาดเล็ก — CSP ที่เข้มงวด (ไม่มี `unsafe-inline`; inline JSON-LD ถูกปักหมุดด้วย sha256), การเจรจา `Accept-Language` + ประเทศ→ภาษา, KV page cache 30 วัน, cron ทำความสะอาดรายวัน — และไม่จำเป็นต้องรู้ว่าข้อมูลถูกสร้างขึ้นมาอย่างไร

**ต้นทุน:** การเปลี่ยนแปลงสกีมา D1 ต้องแก้ไขสองไฟล์ (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`) เป็นการประกันราคาถูก

### สิ่งที่ไม่สามารถต่อรองได้ที่ฝังอยู่พฤติกรรม

- ไม่มีส่วนเกี่ยวข้องกับรัฐบาลสหรัฐฯ; ไม่มีเครื่องหมายราชการ
- การแก้ไขข้อมูลต้นฉบับจะถูกรักษาไว้ ไม่เคยย้อนกลับ
- วิดีโอให้เครดิตกับ DVIDS / AARO
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` ทั่วทั้งไซต์ — สามารถจัดทำดัชนีการค้นหาได้ แต่เลือกไม่เข้าร่วมการขูดข้อมูล AI

ดูสด: https://www.ufolens.com · API: https://www.ufolens.com/api/v1
