# GitHub — 3-ын 2-р нийтлэл · Оролцогчдыг урих / "эхлэхэд тохиромжтой ажлууд"

**Хэрэглэх хүрээ:** хадсан Хэлэлцүүлэг ("Хувь нэмэр оруулах ба эхлэхэд тохиромжтой ажлууд") эсвэл CONTRIBUTING.md-ийн оршил.
**Түлхүүр үгс:** нээлттэй эх, хувь нэмэр оруулах, эхлэхэд тохиромжтой ажил, i18n, нутагшуулалт, OCR, Python, TypeScript, Vitest, pytest, хүртээмж, UAP, нээлттэй өгөгдөл
**Холбоосууд:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## ufolens.com-д хувь нэмэр оруулах нь

[ufolens.com](https://www.ufolens.com) нь АНУ-ын Дайны Департаментын [PURSUE UAP архив](https://www.war.gov/ufo)-ыг хайлт хийх боломжтой, олон хэлний платформ болгон хувиргадаг бөгөөд [олон нийтийн API](https://www.ufolens.com/api/v1)-тай. Энэ нь хоёр хэсгээс бүрддэг — локал Python-ийн оролтын pipeline (`pipeline/`) ба TypeScript/Hono-ийн edge апп (`worker/`) — бөгөөд нэг интерфэйс дээр уулздаг: нийтлэгдсэн SQL + хөрөнгийн багц.

Хувь нэмэр оруулахын тулд танд ямар ч үүлэн орчны нэвтрэх мэдээлэл шаардлагагүй. Pipeline-ийн гол модулиуд нь зөвхөн stdlib-аас хамаардаг ба Worker-ийн тестүүд нь санах ойд суурилсан хадгалалтын эсрэг ажилладаг.

### Суулгац

```bash
# pipeline
python3 -m pytest pipeline/tests/          # бүгд ногоон байх ёстой, pip install шаардлагагүй

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### Тусламж хамгийн их хэрэгтэй байгаа газрууд

**i18n / нутагшуулалт** — `worker/src/i18n/ui-strings.json` нь UI-ийн текстийн эх сурвалж юм. Англи хэлнээс бусад аль ч хэлний нутгийн хэлтэй хүний хяналт маш үнэ цэнэтэй: машины орчуулгын эвгүй хэллэгийг олох, RTL/зохион байгуулалтын асуудлыг засах, хэл сонголтын онцгой тохиолдлуудыг сайжруулах.

**OCR-ийн чанар** — хуучин бичгийн машинаар бичсэн сканнерыг OCR хийхээс өмнө урьдчилан боловсруулалтыг сайжруулах; нээлттэй эхийн хөдөлгүүр болон Tesseract-ийн нөөц хувилбарыг жишиг хуудсууд дээр харьцуулах үнэлгээний систем.

**Хүртээмж** — рендеринг хийсэн хуудсуудыг (`worker/src/render/`) WCAG-ийн эсрэг шалгах; CSP нь хатуу (`unsafe-inline` байхгүй) тул шийдлүүд нь тэр хүрээнд ажиллах ёстой.

**API-ийн хэрэглэхэд ээлтэй байдал** — `worker/src/routes/` — хуудаслалт, шүүлт, OpenAPI-ийн тодорхойлолт, жишээ клиентүүд.

**Pipeline-ийн найдвартай байдал** — илүү сайн алдаагүй ажиллах замууд, явцын тайлагналыг сайжруулах, дельта-илрүүлэлтийн онцгой тохиолдлууд (`pipeline/lib/delta.py`).

**Баримт бичиг** — `docs/20260511/` (繁體中文; `00-*` нь индекс). Дизайны баримт бичгүүдийг англи хэл рүү орчуулахыг хүлээн авна.

### Үндсэн дүрмүүд

- Бүх зам нь харьцангуй байх — төсөл нь машинуудын хооронд зөөгдөх боломжтой байх ёстой. Хатуу кодлогдсон абсолют зам байхгүй.
- Pipeline-ийн *үндсэн* модульд pip-ийн хамаарал нэмж болохгүй. Нэмэлт үе шатууд нь нэмэлт багцуудыг ашиглаж болох бөгөөд тэдгээр байхгүй үед алдаагүй ажиллах ёстой.
- Зөвхөн урагш чиглэсэн төлөвийн машиныг сулруулж болохгүй — энэ бол зардлын дээд хязгаар.
- АНУ-ын засгийн газрын албан ёсны тэмдгийг бүү оруул, мөн эх сурвалжийн засварласан хэсгийг буцааж сэргээдэг зүйл бүү нэм.
- D1 схемийн өөрчлөлт нь **хоёр** файлд нөлөөлнө: `pipeline/lib/manifest_schema.sql` ба `db/schema.sql`.
- Шинэ кодтой хамт тестүүд. Conventional-commit-ийн мессежүүд.

Эхлээд `CLAUDE.md` ба `docs/20260511/00-*`-г уншаад, дараа нь PR хийхээсээ өмнө бүтцийн ямар нэг зүйлийг хэлэлцэх асуудал нээнэ үү.

