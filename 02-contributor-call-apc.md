# GitHub — منشور 2 من 3 · دعوة للمساهمة / "good first issues"

**يُستخدم كـ:** مناقشة مثبتة ("المساهمة و good first issues") أو مقدمة لـ CONTRIBUTING.md.
**كلمات مفتاحية:** open source, contributing, good first issue, i18n, localization, OCR, Python, TypeScript, Vitest, pytest, accessibility, UAP, open data
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة في ufolens.com

[ufolens.com](https://www.ufolens.com) بيحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) التابع لوزارة الحرب الأمريكية لمنصة قابلة للبحث ومتعددة اللغات مع [API عام](https://www.ufolens.com/api/v1). المشروع مقسوم نصين — خط أنابيب استيعاب محلي بـ Python (`pipeline/`) وتطبيق حافة بـ TypeScript/Hono (`worker/`) — بيلتقوا عند واجهة واحدة: حزمة منشورة من SQL + أصول.

ما بتحتاج أي بيانات اعتماد سحابية للمساهمة. الوحدات الأساسية لخط الأنابيب هي stdlib-only فقط واختبارات الـ Worker بتشتغل على تخزين في الذاكرة.

### الإعداد

```bash
# pipeline
python3 -m pytest pipeline/tests/          # لازم كلها تنجح، ما في داعي لتثبيت pip

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### وين المساعدة بتكون مفيدة أكتر شي

**i18n / التوطين** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة من قبل ناطقين أصليين لأي لغة غير الإنجليزية إلها قيمة عالية: اكتشاف الترجمات الآلية الركيكة، إصلاح مشاكل RTL/التخطيط، تحسين الحالات الهامشية للتفاوض اللغوي.

**جودة OCR** — معالجة مسبقة أفضل للمسحات الضوئية القديمة المطبوعة على الآلة الكاتبة قبل OCR؛ إطار تقييم بيقارن المحرك مفتوح المصدر ببديل Tesseract على صفحات عينة.

**إمكانية الوصول** — تدقيق الصفحات المعروضة (`worker/src/render/`) مقابل معايير WCAG؛ الـ CSP صارم (ما في `unsafe-inline`)، فالحلول لازم تشتغل ضمن هاد القيد.

**سهولة استخدام الـ API** — `worker/src/routes/` — التصفح (pagination)، الفلترة، وصف OpenAPI، عملاء مثال.

**متانة خط الأنابيب** — مسارات تراجع رشيق أفضل، تقارير تقدم أحسن، حالات هامشية لكشف التغييرات (`pipeline/lib/delta.py`).

**الوثائق** — `docs/20260511/` (繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة وثائق التصميم للغة الإنجليزية.

### القواعد الأساسية

- كل المسارات نسبية — المشروع لازم يكون قابل للنقل بين الأجهزة. ما في مسارات مطلقة مكتوبة بالكود.
- لا تضيف اعتمادية pip لوحدة *أساسية* في خط الأنابيب. المراحل الاختيارية ممكن تستخدم حزم اختيارية، ولازم تتراجع برشاقة بدونها.
- لا تضعف آلة الحالة ذات المسار الأمامي فقط — هاد هو سقف التكلفة.
- لا تدخل أي شعارات رسمية للحكومة الأمريكية، ولا تضيف أي شي بيعكس الأجزاء المحجوبة من المصدر.
- تغييرات مخطط D1 بتأثر على **ملفين**: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- اختبارات مع الكود الجديد. رسائل commit حسب نمط Conventional Commits.

اقرأ `CLAUDE.md` و `docs/20260511/00-*` أولاً، بعدين افتح issue لمناقشة أي شي هيكلي قبل الـ PR.

