# GitHub — منشور 2 من 3 · دعوة للمساهمين / "مشاكل أولى جيدة"

**الاستخدام:** كمناقشة مثبتة ("المساهمة ومشاكل أولى جيدة") أو مقدمة لملف CONTRIBUTING.md.
**الكلمات المفتاحية:** open source, contributing, good first issue, i18n, localization, OCR, Python, TypeScript, Vitest, pytest, accessibility, UAP, open data
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة في ufolens.com

[ufolens.com](https://www.ufolens.com) يحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) التابع لوزارة الحرب الأمريكية إلى منصة قابلة للبحث ومتعددة اللغات مع [واجهة برمجة تطبيقات عامة](https://www.ufolens.com/api/v1). يتكون من نصفين — مسار معالجة بيانات محلي بلغة Python (`pipeline/`) وتطبيق حافة (edge app) بلغة TypeScript/Hono (`worker/`) — يلتقيان عند واجهة واحدة: حزمة منشورة تحتوي على SQL + أصول.

لست بحاجة إلى أي بيانات اعتماد سحابية للمساهمة. الوحدات الأساسية لمسار المعالجة تعتمد فقط على المكتبة القياسية (stdlib-only) وتعمل اختبارات Worker مقابل تخزين في الذاكرة.

### الإعداد

```bash
# pipeline
python3 -m pytest pipeline/tests/          # should be all green, no pip install needed

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### المجالات الأكثر فائدة للمساعدة

**i18n / التوطين (localization)** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة أي لغة غير إنجليزية من قبل متحدث أصلي لها قيمة عالية: اكتشاف الترجمة الآلية غير الدقيقة، إصلاح مشاكل التنسيق من اليمين إلى اليسار (RTL)/التخطيط، تحسين الحالات الهامشية للتفاوض على اللغة.

**جودة OCR** — معالجة مسبقة أفضل للمستندات الممسوحة ضوئياً القديمة المكتوبة بالآلة الكاتبة قبل التعرف الضوئي على الحروف؛ نظام تقييم يقارن بين المحرك مفتوح المصدر والبديل Tesseract على صفحات عينة.

**إمكانية الوصول (Accessibility)** — مراجعة الصفحات المعروضة (`worker/src/render/`) وفقًا لمعايير WCAG؛ سياسة أمان المحتوى (CSP) صارمة (بدون `unsafe-inline`)، لذا يجب أن تعمل الحلول ضمن هذا الإطار.

**سهولة استخدام API** — `worker/src/routes/` — التقسيم إلى صفحات، التصفية، وصف OpenAPI، عملاء أمثلة.

**متانة مسار المعالجة (Pipeline robustness)** — مسارات تدهور مقبول أكثر، تقارير تقدم أفضل، حالات هامشية في كشف التغييرات (`pipeline/lib/delta.py`).

**المستندات (Docs)** — `docs/20260511/` (繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة مستندات التصميم إلى اللغة الإنجليزية.

### القواعد الأساسية

- كل المسارات نسبية — يجب أن يكون المشروع قابلاً للنقل بين الأجهزة. لا توجد مسارات مطلقة مكتوبة بشكل ثابت.
- لا تضف تبعية pip إلى وحدة *أساسية* في مسار المعالجة. قد تستخدم المراحل الاختيارية حزمًا اختيارية، ويجب أن تتدهور بشكل مقبول بدونها.
- لا تضعف آلية الحالة التقدمية فقط — هذا هو سقف التكلفة.
- لا تقدم شعارات حكومية أمريكية رسمية، ولا تضف أي شيء يلغي التنقيحات المصدرية.
- تغييرات مخطط D1 تلمس ملفين **اثنين**: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- اختبارات مع الكود الجديد. رسائل الالتزام التقليدية (Conventional-commit).

اقرأ `CLAUDE.md` و `docs/20260511/00-*` أولاً، ثم افتح مشكلة (issue) لمناقشة أي شيء هيكلي قبل طلب السحب (PR).
