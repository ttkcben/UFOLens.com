# GitHub — المنشور 1 من 3 · إعلان الإصدار / فقرة README

**يُستخدم كـ:** نص أساسي لإصدار GitHub، أو مناقشة مثبتة، أو في أعلى ملف README للمستودع.
**الكلمات المفتاحية:** UAP، UFO، أرشيف PURSUE، وثائق رفعت عنها السرية، بيانات مفتوحة، بحث نصي كامل، OCR، ترجمة آلية، LLM محلي، Ollama، حوسبة الحافة، API عام، Hono، TypeScript، Python
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — منصة متعددة اللغات وقابلة للبحث لأرشيف PURSUE UAP

**الرابط المباشر:** https://www.ufolens.com · **API:** https://www.ufolens.com/api/v1 · **الأرشيف المصدر:** https://www.war.gov/ufo

`ufolens.com` يعيد نشر أرشيف **PURSUE** التابع لوزارة الحرب الأمريكية للسجلات التي رفعت عنها السرية بخصوص UAP / UFO كمنصة معرفية: بحث نصي كامل، ترجمة آلية عبر المجموعة الكاملة، استكشاف عبر الخريطة والخط الزمني، وواجهة برمجة تطبيقات JSON عامة. الوثائق المصدر هي من أعمال الحكومة الفيدرالية الأمريكية وتعتبر ضمن الملكية العامة داخل الولايات المتحدة ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). هذا المشروع **لا ينتمي إلى حكومة الولايات المتحدة**، ولا يستخدم أي شارات رسمية، ولا يعكس أبدًا أي تنقيحات.

### البنية الهندسية

```
الجهاز المحلي (Apple Silicon، IP سكني)             شبكة الحافة
─────────────────────────────────────────           ─────────────────────────
pipeline/ (Python 3.10، النواة stdlib-only)        worker/ (TypeScript، Hono.js)
  fetch → OCR → translate → publish (تقدمي فقط)     /{lang}/...   صفحات
  OCR: محرك مفتوح المصدر (Tesseract CLI كبديل)      /api/v1/...   API عام
  translate / NER: LLM محلي (Gemma عبر Ollama)     /admin        وحدة تحكم المشغل
  state: بيان SQLite                                مدعوم من: قاعدة بيانات SQL على الحافة، تخزين
        │                                              كائني (ملفات PDF المصدر)، ذاكرة تخزين مؤقت KV
        └── ينشر حزمة: SQL + بيان الأصول + قائمة مسح ذاكرة التخزين المؤقت ──┘
```

- **تكلفة صفرية لكل وثيقة على الذكاء الاصطناعي السحابي.** يتم تشغيل OCR والترجمة محليًا؛ تضمن آلة الحالة التقدمية فقط (`discovered → downloaded → ocr_done → translated → published`) عدم إعادة معالجة أي وثيقة ما لم تتغير.
- **نواة خط الأنابيب لا تحتوي على أي تبعيات من طرف ثالث** — وحدات التحليل / البيان / دلتا تعمل وتُختبر على بيئة Python نظيفة دون تثبيت أي شيء عبر pip؛ مراحل OCR/الترجمة تتدهور برشاقة عند غياب الحزم الاختيارية.
- **موقع الحافة** يطبق ترويسات أمان صارمة + CSP (لا يوجد `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض اللغة عبر `Accept-Language` + ربط البلدان، ذاكرة تخزين مؤقت للصفحات في KV لمدة 30 يومًا، ومهمة صيانة يومية مجدولة.
- **تحديثات تدريجية:** يكشف كاشف دلتا الفروق في فهرس المصدر ويغذي فقط التغييرات مرة أخرى إلى خط الأنابيب.

### للمطورين

توفر واجهة برمجة التطبيقات العامة على https://www.ufolens.com/api/v1 الوثائق والبيانات الوصفية بصيغة JSON. الوصول المجهول محدود بمعدل معين؛ اطلب مفتاحًا لفئات الباحثين/المطورين. راجع قسم API على الموقع لمعرفة نقاط النهاية والحدود.

### الحالة

الكود مكتمل؛ الموقع منشور على https://www.ufolens.com. يتم ملء قاعدة بيانات الإنتاج عن طريق تشغيل خط الأنابيب دون اتصال بالإنترنت ونشر الحزمة (`cli_publish run --remote`). وثائق التصميم الكاملة موجودة في `docs/20260511/`.

### الترخيص / الحدود

- الوثائق المصدر: أعمال الحكومة الفيدرالية الأمريكية، ملكية عامة داخل الولايات المتحدة.
- الكود الخاص بهذه المنصة: انظر `LICENSE`.
- يرسل الموقع `Tdm-Reservation: 1` و `X-Robots-Tag: noai, noimageai` — قابل للفهرسة من قبل محركات البحث، مع إلغاء الاشتراك في تدريب/كشط الذكاء الاصطناعي.
- لقطات الفيديو منسوبة إلى DVIDS / AARO ولا يدعي هذا المشروع ملكيتها.

نرحب بالمشاكل وطلبات السحب (PRs). يرجى قراءة `CLAUDE.md` و `docs/20260511/00-*` قبل فتح أي تغييرات هيكلية.
