# GitHub — Публікацыя 2 з 3 · Заклік да ўдзелу / "добрыя першыя задачы"

**Выкарыстанне:** як замацаванае абмеркаванне ("Удзел і добрыя першыя задачы") або ўступ да CONTRIBUTING.md.
**Ключавыя словы:** адкрыты зыходны код, удзел, добрая першая задача, i18n, лакалізацыя, OCR, Python, TypeScript, Vitest, pytest, даступнасць, UAP, адкрытыя даныя
**Гіперспасылкі:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## Удзел у распрацоўцы ufolens.com

[ufolens.com](https://www.ufolens.com) ператварае [архіў UAP PURSUE](https://www.war.gov/ufo) Ваеннага дэпартамента ЗША ў шматмоўную платформу з магчымасцю пошуку і [публічным API](https://www.ufolens.com/api/v1). Ён складаецца з дзвюх частак — лакальнага канвеера прыёму даных на Python (`pipeline/`) і пагранічнага прыкладання на TypeScript/Hono (`worker/`), якія сустракаюцца на адзіным інтэрфейсе: апублікаваным пакеце SQL + рэсурсы.

Вам не патрэбны ніякія хмарныя ўліковыя даныя, каб зрабіць унёсак. Асноўныя модулі канвеера выкарыстоўваюць толькі стандартную бібліятэку, а тэсты Worker працуюць з выкарыстаннем сховішча ў памяці.

### Наладка

```bash
# канвеер
python3 -m pytest pipeline/tests/          # усе павінны быць зялёнымі, не патрабуецца ўстаноўка pip

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### Дзе дапамога найбольш карысная

**i18n / лакалізацыя** — `worker/src/i18n/ui-strings.json` з'яўляецца крыніцай радкоў інтэрфейсу. Праверка носьбітам любой неангламоўнай лакалі вельмі каштоўная: выявіць нязграбны машынны пераклад, выправіць праблемы з RTL/макетам, палепшыць крайнія выпадкі вызначэння мовы.

**Якасць OCR** — паляпшэнне папярэдняй апрацоўкі старых машынапісных сканаў перад OCR; ацэначны комплекс для параўнання рухавіка з адкрытым кодам з запасным варыянтам Tesseract на ўзорах старонак.

**Даступнасць** — аўдыт адлюстраваных старонак (`worker/src/render/`) на адпаведнасць WCAG; CSP строгі (без `unsafe-inline`), таму рашэнні павінны працаваць у гэтых межах.

**Эрганоміка API** — `worker/src/routes/` — пагінацыя, фільтрацыя, апісанне OpenAPI, прыклады кліентаў.

**Надзейнасць канвеера** — больш шляхоў карэктнай дэградацыі, лепшае паведамленне пра ход выканання, крайнія выпадкі выяўлення дэльты (`pipeline/lib/delta.py`).

**Дакументацыя** — `docs/20260511/` (繁體中文; `00-*` - гэта індэкс). Пераклады праектнай дакументацыі на англійскую мову вітаюцца.

### Асноўныя правілы

- Усе шляхі адносныя — праект павінен быць пераносным паміж машынамі. Ніякіх жорстка закадзіраваных абсалютных шляхоў.
- Не дадавайце залежнасць pip у *асноўны* модуль канвеера. Неабавязковыя этапы могуць выкарыстоўваць неабавязковыя пакеты і павінны карэктна дэградаваць без іх.
- Не аслабляйце машыну станаў 'толькі наперад' — гэта столь выдаткаў.
- Не дадавайце афіцыйную сімволіку ўрада ЗША і нічога, што адмяняе рэдагаванне зыходных дакументаў.
- Змены схемы D1 закранаюць **два** файлы: `pipeline/lib/manifest_schema.sql` і `db/schema.sql`.
- Тэсты з новым кодам. Паведамленні аб фіксацыі ў стылі Conventional Commits.

Спачатку прачытайце `CLAUDE.md` і `docs/20260511/00-*`, а затым адкрыйце issue, каб абмеркаваць любыя структурныя змены перад стварэннем PR.

