# GitHub — المنشور 2 من 3 · دعوة للمساهمين / "مهام مناسبة للبداية"

**الاستخدام:** كمناقشة مثبتة ("المساهمة ومهام مناسبة للبداية") أو مقدمة لملف CONTRIBUTING.md.
**الكلمات المفتاحية:** open source, contributing, good first issue, i18n, localization, OCR, Python, TypeScript, Vitest, pytest, accessibility, UAP, open data
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة في ufolens.com

[ufolens.com](https://www.ufolens.com) يحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) التابع لوزارة الحرب الأمريكية إلى منصة قابلة للبحث ومتعددة اللغات مع [API عام](https://www.ufolens.com/api/v1). يتكون المشروع من نصفين — خط أنابيب استيعاب محلي بلغة Python (`pipeline/`) وتطبيق حافة بلغة TypeScript/Hono (`worker/`) — يلتقيان عند واجهة واحدة: حزمة منشورة تحتوي على SQL + أصول.

لا تحتاج إلى أي بيانات اعتماد سحابية للمساهمة. الوحدات الأساسية لخط الأنابيب تعتمد فقط على المكتبة القياسية (stdlib-only) واختبارات الـ Worker تعمل مقابل تخزين في الذاكرة.

### الإعداد

```bash
# pipeline
python3 -m pytest pipeline/tests/          # should be all green, no pip install needed

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### أفضل المجالات للمساعدة

**التدويل / التوطين (i18n / localization)** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة أي لغة غير الإنجليزية من قبل متحدث أصلي لها قيمة عالية جدًا: اكتشاف الترجمات الآلية الركيكة، إصلاح مشاكل التخطيط للغات من اليمين إلى اليسار (RTL)، وتحسين الحالات الهامشية في التفاوض على اللغة.

**جودة OCR** — تحسين المعالجة المسبقة للمستندات القديمة المطبوعة بالآلة الكاتبة قبل OCR؛ بناء نظام تقييم يقارن بين المحرك مفتوح المصدر والبديل Tesseract على صفحات عينة.

**إمكانية الوصول (Accessibility)** — مراجعة الصفحات المعروضة (`worker/src/render/`) وفقًا لمعايير WCAG؛ الـ CSP صارم (لا يوجد `unsafe-inline`)، لذا يجب أن تعمل الحلول ضمن هذا القيد.

**سهولة استخدام الـ API** — `worker/src/routes/` — التقسيم الصفحي (pagination)، التصفية (filtering)، وصف OpenAPI، وعملاء مثال.

**متانة خط الأنابيب** — إضافة مسارات أكثر للتكيف برشاقة عند الفشل، تحسين تقارير التقدم، ومعالجة الحالات الهامشية في كشف التغييرات (`pipeline/lib/delta.py`).

**الوثائق** — `docs/20260511/` (繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة وثائق التصميم إلى اللغة الإنجليزية.

### القواعد الأساسية

- كل المسارات نسبية — يجب أن يكون المشروع قابلاً للنقل بين الأجهزة. لا توجد مسارات مطلقة مكتوبة بشكل ثابت.
- لا تضف اعتمادية pip إلى وحدة *أساسية* في خط الأنابيب. يمكن للمراحل الاختيارية استخدام حزم اختيارية، ويجب أن تتكيف برشاقة عند غيابها.
- لا تضعف آلية الحالة ذات المسار الأمامي الإجباري — فهذا هو سقف التكلفة.
- لا تدخل أي شعارات رسمية للحكومة الأمريكية، ولا تضف أي شيء يلغي التنقيحات الموجودة في المصدر.
- تغييرات مخطط D1 تتطلب تعديل ملفين: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- إضافة اختبارات مع أي كود جديد. استخدام رسائل commit تتبع أسلوب Conventional Commits.

اقرأ `CLAUDE.md` و `docs/20260511/00-*` أولاً، ثم افتح قضية (issue) لمناقشة أي تغيير هيكلي قبل إرسال طلب السحب (PR).

