# GitHub — منشور 3 من 3 · ملاحظات على الهيكل العام (مناقشة بأسلوب ADR)

**يُستخدم كـ:** مناقشة تحت "عرض ومشاركة" / "الهيكل العام"، أو كنواة لـ ADR فى `docs/`.
**الكلمات المفتاحية:** architecture، ADR، آلية حالة ذات اتجاه واحد للأمام، LLM محلى، Ollama، OCR، حوسبة الحافة، CSP، ترويسات الأمان، data pipeline، هندسة التكلفة، بيان SQLite، D1، R2، KV
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## ليه ufolens.com مبنى بالطريقة دى

ملاحظات على القرارات الثلاثة اللى شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث ومتعددة اللغات لـ [أرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات / الاعتراضات.

### 1. الـ pipeline عبارة عن آلية حالة ذات اتجاه واحد للأمام — عن قصد

الحالات: `مكتشف ← محمل ← تم عمل ocr ← مترجم ← منشور`. المستند بيتحرك للأمام بس، وفقط لما يكون فيه شغل يتعمل. المحتوى المنشور مش بيتعاد معالجته أبداً إلا لو كاشف التغييرات شاف إن المصدر اتغير فعلاً.

**السبب:** الـ OCR + الترجمة هما العمليات المكلفة، والأرشيف بيكبر مع الوقت. أى pipeline "بيعيد تشغيل كل حاجة عشان يبقى فى الأمان" تكلفته غير محدودة. إننا نلغى إمكانية الرجوع للخلف بيخلى الفواتير الخرافية مستحيلة. سقف التكلفة هو خاصية فى مخطط الحالة نفسه، مش بيعتمد على يقظة المشغل.

**المقابل:** عمليات ترحيل المخطط (schema migrations) وإعادة المعالجة عن قصد بتكون صعبة بشكل متعمد. وده مقابل مقبول.

### 2. الـ OCR والترجمة بيشتغلوا على نموذج لغوى محلى، مش على API سحابى

الـ OCR: محرك مفتوح المصدر، والبديل هو Tesseract CLI. الترجمة + NER: نموذج Gemma عن طريق Ollama، على لابتوب Apple Silicon.

**السبب:** تكلفة هامشية صفر لكل مستند؛ قابلة للتكرار (نموذج ومطالبات ثابتة)؛ وخطوة الجلب أصلاً لازم تشتغل من عنوان IP منزلى (لأن المصدر وراه Akamai Bot Manager — `curl` بيترد عليه بـ 403)، فكده كده فيه لابتوب فى الموضوع.

**المقابل:** جودة الترجمة أقل من أى نموذج متطور. بالنسبة لأرشيف مرجعى الإنجليزية الأصلية فيه على بعد ضغطة واحدة، ده مقبول. إحنا مش بندعى إن الترجمات دى رسمية أو معتمدة.

### 3. النصين بيشتركوا فى واجهة واحدة بس: حزمة منشورة

الـ pipeline مش بيكتب أبداً فى قاعدة بيانات الإنتاج مباشرة. هو بيصدر `{ SQL، بيان الأصول، قائمة بحذف الكاش }`. عملية "النشر" = تطبيق الحزمة دى للأمام (دفع الـ SQL لقاعدة بيانات SQL على الحافة، مزامنة الأصول مع تخزين الكائنات، حذف مفاتيح الكاش المحددة).

**السبب:** الجانب المحلى وجانب الحافة ممكن يتطوروا بشكل مستقل؛ الحزمة قابلة للمراجعة؛ وشكل "نشر البيانات" هو هو كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — CSP صارم (مفيش `unsafe-inline`؛ الـ JSON-LD المضمن مثبت بـ sha256)، والتفاوض على اللغة بيتم عن طريق `Accept-Language` + ربط الدولة باللغة، وذاكرة تخزين مؤقت للصفحات فى KV لمدة 30 يوم، و cron يومى للصيانة — ومش محتاج يعرف أبداً البيانات دى اتعملت إزاى.

**المقابل:** أى تغيير فى مخطط D1 بيأثر على ملفين (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`). تأمين رخيص.

### حاجات غير قابلة للتفاوض ومدمجة فى السلوك

- غير تابع لحكومة الولايات المتحدة؛ لا توجد شارات رسمية.
- يتم الحفاظ على المعلومات المحجوبة فى المصدر، ولا يتم كشفها أبداً.
- الفيديو منسوب إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من محركات البحث، ومعطل خاصية الكشط من نماذج الذكاء الاصطناعى.

الموقع المباشر: https://www.ufolens.com · الـ API: https://www.ufolens.com/api/v1

