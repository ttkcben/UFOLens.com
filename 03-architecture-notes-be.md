# GitHub — Публікацыя 3 з 3 · Архітэктурныя нататкі (абмеркаванне ў стылі ADR)

**Выкарыстанне:** як абмеркаванне ў раздзеле "Show and tell" / "Архітэктура" або як пачатковы ADR для `docs/`.
**Ключавыя словы:** архітэктура, ADR, машына станаў 'толькі наперад', лакальная LLM, Ollama, OCR, пагранічныя вылічэнні, CSP, загалоўкі бяспекі, канвеер даных, інжынірынг выдаткаў, маніфест SQLite, D1, R2, KV
**Гіперспасылкі:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## Чаму ufolens.com пабудаваны менавіта так

Нататкі пра тры рашэнні, якія сфармавалі [ufolens.com](https://www.ufolens.com) (перабудаваны, шматмоўны архіў [PURSUE UAP](https://www.war.gov/ufo) з магчымасцю пошуку). Каментарыі / крытыка вітаюцца.

### 1. Канвеер — гэта машына станаў 'толькі наперад' — і гэта наўмысна

Станы: `discovered → downloaded → ocr_done → translated → published`. Дакумент рухаецца толькі наперад, і толькі тады, калі ёсць праца, якую трэба зрабіць. Апублікаваны кантэнт ніколі не апрацоўваецца паўторна, калі дэтэктар дэльты не бачыць, што крыніца сапраўды змянілася.

**Чаму:** OCR + пераклад — гэта дарагія аперацыі, і архіў з часам расце. Канвеер, які "паўторна запускае ўсё для бяспекі", мае неабмежаваныя выдаткі. Зрабіць пераходы назад немагчымымі робіць немагчымым і бескантрольны рост рахункаў. Столь выдаткаў з'яўляецца ўласцівасцю графа станаў, а не пільнасці аператара.

**Кошт:** міграцыі схемы і наўмысная паўторная апрацоўка наўмысна нязручныя. Прымальны кампраміс.

### 2. OCR і пераклад працуюць на лакальнай LLM, а не ў хмарным API

OCR: рухавік з адкрытым кодам, Tesseract CLI як запасны варыянт. Пераклад + NER: Gemma праз Ollama, на ноўтбуку Apple Silicon.

**Чаму:** нулявы гранічны кошт за дакумент; узнаўляльнасць (фіксаваная мадэль + падказкі); і крок выбаркі ўжо павінен выконвацца з бытавога IP (крыніца знаходзіцца за Akamai Bot Manager — `curl` атрымлівае 403), таму ноўтбук у любым выпадку ўдзельнічае ў працэсе.

**Кошт:** якасць перакладу ніжэйшая, чым у перадавых мадэляў. Для даведачнага корпуса, дзе арыгінал на англійскай мове заўсёды даступны ў адзін клік, гэта нармальна. Мы не сцвярджаем, што пераклады з'яўляюцца аўтарытэтнымі.

### 3. Дзве паловы маюць роўна адзін агульны інтэрфейс: апублікаваны пакет

Канвеер ніколі не запісвае даныя непасрэдна ў прадакшн-базу даных. Ён выпускае `{ SQL, маніфест рэсурсаў, спіс ачысткі кэша }`. "Публікацыя" = прымяніць гэты пакет наперад (адправіць SQL у пагранічную SQL DB, сінхранізаваць рэсурсы са сховішчам аб'ектаў, ачысціць названыя ключы кэша).

**Чаму:** лакальны і пагранічны бакі могуць развівацца незалежна; пакет можна праверыць; і "разгортванне даных" кожны раз мае аднолькавую форму. Worker — гэта невялікае прыкладанне на TypeScript/Hono — строгі CSP (без `unsafe-inline`; убудаваны JSON-LD замацаваны праз sha256), вызначэнне мовы праз `Accept-Language` + краіна→мова, 30-дзённы кэш старонак у KV, штодзённая задача абслугоўвання па cron — і яму ніколі не трэба ведаць, як былі створаны даныя.

**Кошт:** змена схемы D1 закранае два файлы (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`). Танная страхоўка.

### Бескампрамісныя прынцыпы, убудаваныя ў паводзіны

- Не звязаны з урадам ЗША; без афіцыйнай сімволікі.
- Рэдагаванне зыходных кодаў захоўваецца, ніколі не адмяняецца.
- Відэа аднесена да DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` па ўсім сайце — індэксуецца пошукавымі сістэмамі, адключаны для збору даных AI.

Сайт: https://www.ufolens.com · API: https://www.ufolens.com/api/v1

