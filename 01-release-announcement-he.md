# GitHub — פוסט 1 מתוך 3 · הכרזת שחרור / בלוק README

**שימוש:** גוף ההודעה עבור שחרור (Release) ב-GitHub, דיון נעוץ, או החלק העליון של קובץ ה-README במאגר.
**מילות מפתח:** UAP, UFO, ארכיון PURSUE, מסמכים מסווגים, נתונים פתוחים, חיפוש טקסט מלא, OCR, תרגום מכונה, LLM מקומי, Ollama, מחשוב קצה, API ציבורי, Hono, TypeScript, Python
**היפר-קישורים:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — פלטפורמה רב-לשונית וניתנת לחיפוש עבור ארכיון ה-PURSUE UAP

**קישור לאתר:** https://www.ufolens.com · **API:** https://www.ufolens.com/api/v1 · **ארכיון מקור:** https://www.war.gov/ufo

`ufolens.com` מפרסם מחדש את ארכיון **PURSUE** של מחלקת המלחמה של ארה"ב, המכיל רשומות UAP / UFO שהוסר סיווגן, כפלטפורמת ידע: חיפוש טקסט מלא, תרגום מכונה על פני כל המאגר, חקר מפות וצירי זמן, ו-API ציבורי בפורמט JSON. מסמכי המקור הם יצירות של הממשל הפדרלי של ארה"ב, ובתוך ארה"ב הם נמצאים בנחלת הכלל ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). פרויקט זה **אינו משויך לממשלת ארה"ב**, אינו משתמש בסמלים רשמיים, ולעולם אינו מבטל צנזורה (redactions).

### ארכיטקטורה

```
מכונה מקומית (Apple Silicon, IP ביתי)          רשת קצה (Edge network)
─────────────────────────────────────────           ─────────────────────────
pipeline/  (Python 3.10, ליבה מבוססת stdlib)       worker/  (TypeScript, Hono.js)
  fetch → OCR → translate → publish  (קדימה בלבד)    /{lang}/...   עמודים
  OCR: מנוע קוד פתוח (Tesseract CLI כחלופה)         /api/v1/...   API ציבורי
  translate / NER: LLM מקומי (Gemma דרך Ollama)      /admin        קונסולת מפעיל
  state: מניפסט SQLite                            מגובה על ידי: מסד נתונים SQL בקצה,
        │                                              אחסון אובייקטים (קובצי PDF מקוריים), מטמון KV
        └── מפרסם חבילה: SQL + מניפסט נכסים + רשימת מחיקת מטמון ──┘
```

- **עלות אפסית של AI-ענן פר-מסמך.** תהליכי OCR ותרגום רצים באופן מקומי; מכונת המצבים המתקדמת-קדימה-בלבד (`discovered → downloaded → ocr_done → translated → published`) מבטיחה שמסמך לא יעובד מחדש אלא אם כן הוא השתנה.
- **ליבת ה-pipeline חסרת תלות בספריות צד-שלישי** — מודולי הניתוח / מניפסט / דלתא רצים ונבדקים על גרסת Python נקייה ללא התקנות pip; שלבי ה-OCR/תרגום מציעים כשל-חינני (degrade gracefully) כאשר חבילות אופציונליות חסרות.
- **אתר הקצה (Edge)** מיישם כותרות אבטחה (headers) ו-CSP מחמירים (ללא `unsafe-inline`; קוד JSON-LD מוטבע מאומת באמצעות sha256), משא ומתן על שפה באמצעות `Accept-Language` + מיפוי מדינות, מטמון עמודים ב-KV למשך 30 יום, ו-cron יומי לתחזוקה.
- **עדכונים תוספתיים (Incremental):** גלאי דלתא (delta detector) משווה את אינדקס המקור ומזין רק שינויים בחזרה ל-pipeline.

### למפתחים

ה-API הציבורי בכתובת https://www.ufolens.com/api/v1 מחזיר מסמכים ו-metadata בפורמט JSON. גישה אנונימית מוגבלת בקצבה; בקשו מפתח עבור רמות גישה לחוקרים/מפתחים. עיינו בסעיף ה-API באתר למידע על נקודות קצה (endpoints) ומגבלות.

### סטטוס

הקוד הושלם; האתר פרוס בכתובת https://www.ufolens.com. מסד הנתונים של סביבת הייצור (production) מאוכלס על ידי הרצת ה-pipeline במצב לא-מקוון (offline) ופרסום החבילה קדימה (`cli_publish run --remote`). מסמכי התכנון המלאים נמצאים ב-`docs/20260511/`.

### רישיון / גבולות

- מסמכי מקור: יצירות של הממשל הפדרלי של ארה"ב, בנחלת הכלל בתוך ארה"ב.
- הקוד של פלטפורמה זו: ראו `LICENSE`.
- האתר שולח `Tdm-Reservation: 1` ו-`X-Robots-Tag: noai, noimageai` — ניתן לאינדוקס על ידי מנועי חיפוש, אך מביע התנגדות לאימון בינה מלאכותית וגירוד נתונים (scraping).
- קטעי וידאו מיוחסים ל-DVIDS / AARO ואין לפרויקט זה כל טענה לבעלות עליהם.

נשמח לקבל דיווחי תקלות (Issues) ובקשות משיכה (PRs). אנא קראו את `CLAUDE.md` ואת `docs/20260511/00-*` לפני פתיחת שינויים מבניים.

