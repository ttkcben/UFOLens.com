# GitHub — پست ۳ از ۳ · یادداشت‌های معماری (Discussion به سبک ADR)

**استفاده به عنوان:** یک Discussion زیر "نمایش و گفتگو" / "معماری"، یا به عنوان پایه `docs/` ADR.
**کلمات کلیدی:** معماری، ADR، ماشین وضعیت فقط رو به جلو، LLM محلی، Ollama، OCR، محاسبات لبه، CSP، هدرهای امنیتی، پایپ‌لاین داده، مهندسی هزینه، مانیفست SQLite، D1، R2، KV
**لینک‌ها:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## چرا ufolens.com به این شکل ساخته شده است

یادداشت‌هایی در مورد سه تصمیمی که [ufolens.com](https://www.ufolens.com) (بازسازی قابل جستجو و چندزبانه [آرشیو PURSUE UAP](https://www.war.gov/ufo)) را شکل دادند. از نظرات / مخالفت‌ها استقبال می‌شود.

### ۱. پایپ‌لاین یک ماشین وضعیت فقط رو به جلو است — عمداً

وضعیت‌ها: `کشف‌شده → دانلودشده → ocr_انجام‌شده → ترجمه‌شده → منتشرشده`. یک سند فقط به جلو حرکت می‌کند، و فقط زمانی که کاری برای انجام دادن وجود داشته باشد. محتوای منتشر شده هرگز مجدداً پردازش نمی‌شود، مگر اینکه یک آشکارساز دلتا ببیند که منبع واقعاً تغییر کرده است.

**چرا:** OCR + ترجمه عملیات پرهزینه هستند و آرشیو با گذشت زمان رشد می‌کند. پایپ‌لاینی که "برای اطمینان همه چیز را دوباره اجرا می‌کند" هزینه نامحدودی دارد. غیرممکن کردن انتقال‌های به عقب، یک صورتحساب خارج از کنترل را غیرممکن می‌کند. سقف هزینه یک ویژگی از گراف وضعیت است، نه از هوشیاری اپراتور.

**هزینه:** مهاجرت‌های شما و پردازش مجدد عمدی، به طور آگاهانه‌ای دشوار هستند. یک بده‌بستان قابل قبول.

### ۲. OCR و ترجمه روی یک LLM محلی اجرا می‌شوند، نه یک API ابری

OCR: موتور منبع باز، با جایگزینی Tesseract CLI. ترجمه + NER: Gemma از طریق Ollama، روی یک لپ‌تاپ Apple Silicon.

**چرا:** هزینه حاشیه‌ای صفر برای هر سند؛ قابل تکرار (مدل ثابت + پرامپت‌ها)؛ و مرحله واکشی از قبل باید از یک IP مسکونی اجرا شود (منبع پشت Akamai Bot Manager است — `curl` خطای ۴۰۳ می‌گیرد)، بنابراین به هر حال یک لپ‌تاپ در حلقه قرار دارد.

**هزینه:** کیفیت ترجمه پایین‌تر از یک مدل پیشرو است. برای یک مجموعه مرجع که در آن نسخه اصلی انگلیسی همیشه با یک کلیک فاصله دارد، این مسئله قابل قبول است. ما ادعا نمی‌کنیم که ترجمه‌ها معتبر هستند.

### ۳. دو نیمه دقیقاً یک رابط را به اشتراک می‌گذارند: یک بسته منتشر شده

پایپ‌لاین هرگز مستقیماً در پایگاه داده تولیدی نمی‌نویسد. آن `{ SQL، مانیفست دارایی، لیست پاک‌سازی کش }` را منتشر می‌کند. "انتشار" = اعمال آن بسته به جلو (ارسال SQL به پایگاه داده SQL لبه، همگام‌سازی دارایی‌ها با ذخیره‌سازی اشیاء، پاک‌سازی کلیدهای کش نام‌برده شده).

**چرا:** سمت محلی و سمت لبه می‌توانند به طور مستقل تکامل یابند؛ بسته قابل بررسی است؛ و "استقرار داده" هر بار به یک شکل است. Worker یک برنامه کوچک TypeScript/Hono است — CSP سختگیرانه (بدون `unsafe-inline`؛ JSON-LD درون خطی با sha256 پین شده است)، مذاکره `Accept-Language` + کشور به زبان، کش صفحه KV ۳۰ روزه، کرون روزانه برای نگهداری — و هرگز نیازی به دانستن نحوه ساخت داده‌ها ندارد.

**هزینه:** یک تغییر در شمای D1 دو فایل را تحت تأثیر قرار می‌دهد (`pipeline/lib/manifest_schema.sql`، `db/schema.sql`). بیمه‌ای ارزان.

### موارد غیرقابل مذاکره که در رفتار تعبیه شده‌اند

- وابسته به دولت ایالات متحده نیست؛ بدون نشان رسمی.
- ویرایش‌های منبع حفظ می‌شوند، هرگز برگردانده نمی‌شوند.
- ویدیو به DVIDS / AARO نسبت داده شده است.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` در سراسر سایت — قابل ایندکس برای جستجو، انصراف داده شده از خزش هوش مصنوعی.

پخش زنده: https://www.ufolens.com · API: https://www.ufolens.com/api/v1

