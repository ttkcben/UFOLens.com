# GitHub — منشور 2 من 3 · دعوة للمساهمين / "أول issues كويسة"

**يُستخدم كـ:** مناقشة مثبتة ("المساهمة وأول issues كويسة") أو مقدمة لـ CONTRIBUTING.md.
**الكلمات المفتاحية:** مفتوح المصدر، مساهمة، good first issue، i18n، توطين، OCR، Python، TypeScript، Vitest، pytest، إمكانية الوصول، UAP، بيانات مفتوحة
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة فى ufolens.com

[ufolens.com](https://www.ufolens.com) بيحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) الخاص بوزارة الحرب الأمريكية لمنصة قابلة للبحث ومتعددة اللغات مع [API عام](https://www.ufolens.com/api/v1). المشروع نصين — pipeline استيعاب محلى بـ Python (`pipeline/`) وتطبيق حافة (edge app) بـ TypeScript/Hono (`worker/`) — والاتنين بيتقابلوا عند واجهة واحدة: حزمة منشورة فيها SQL + أصول.

مش محتاج أى بيانات دخول سحابية عشان تساهم. الوحدات الأساسية فى الـ pipeline بتعتمد على المكتبة القياسية بس واختبارات الـ Worker بتشتغل على مساحة تخزين فى الذاكرة.

### الإعداد

```bash
# الـ pipeline
python3 -m pytest pipeline/tests/          # المفروض كلها تعدى، مش محتاج pip install

# الـ worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### الأماكن اللى المساعدة فيها هتكون مفيدة جداً

**i18n / التوطين (localization)** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة أى لغة غير الإنجليزية من متحدث أصلى ليها قيمة عالية جداً: عشان نصلح أى ترجمة آلية شكلها غريب، ونظبط مشاكل الـ RTL/التصميم، ونحسن الحالات الخاصة فى التفاوض على اللغة.

**جودة الـ OCR** — معالجة مسبقة أفضل للمستندات القديمة المطبوعة بالآلة الكاتبة قبل الـ OCR؛ إطار تقييم يقارن بين المحرك مفتوح المصدر و Tesseract كبديل على صفحات عينة.

**إمكانية الوصول (Accessibility)** — مراجعة الصفحات المعروضة (`worker/src/render/`) حسب معايير WCAG؛ الـ CSP صارم (مفيش `unsafe-inline`)، فالحلول لازم تشتغل فى الإطار ده.

**سهولة استخدام الـ API (API ergonomics)** — `worker/src/routes/` — التقسيم لصفحات (pagination)، الفلترة، وصف OpenAPI، وعملاء كمثال.

**متانة الـ pipeline** — مسارات تدهور أرشق (graceful-degradation)، تقارير تقدم أفضل، والحالات الخاصة فى اكتشاف التغييرات (`pipeline/lib/delta.py`).

**المستندات** — `docs/20260511/` (繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة مستندات التصميم للغة الإنجليزية.

### القواعد الأساسية

- كل المسارات نسبية — المشروع لازم يكون قابل للنقل بين الأجهزة. مفيش مسارات مطلقة مكتوبة بشكل ثابت.
- متضفش أى اعتمادية pip لوحدة *أساسية* فى الـ pipeline. المراحل الاختيارية ممكن تستخدم حزم اختيارية، ولازم تتدهور برشاقة من غيرها.
- متضعفش آلية الحالة ذات الاتجاه الواحد للأمام — هى دى اللى بتحدد سقف التكلفة.
- متدخلش أى شارات رسمية لحكومة الولايات المتحدة، ومتضفش أى حاجة بتكشف المعلومات المحجوبة فى المصدر.
- أى تغيير فى مخطط D1 بيأثر على **ملفين**: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- اختبارات مع الكود الجديد. رسائل commit تكون حسب الـ Conventional Commits.

اقرأ `CLAUDE.md` و `docs/20260511/00-*` الأول، وبعدين افتح issue عشان نناقش أى حاجة هيكلية قبل الـ PR.

