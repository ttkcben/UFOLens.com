# GitHub — المنشور 3 من 3 · ملاحظات حول البنية (مناقشة بأسلوب ADR)

**استخدام كـ:** مناقشة تحت "Show and tell" / "Architecture"، أو كبذرة لـ ADR ف `docs/`.
**الكلمات المفتاحية:** architecture, ADR, forward-only state machine, local LLM, Ollama, OCR, edge computing, CSP, security headers, data pipeline, cost engineering, SQLite manifest, D1, R2, KV
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## علاش ufolens.com مبني بهاد الطريقة

ملاحظات على القرارات الثلاثة اللي شكلات [ufolens.com](https://www.ufolens.com) (إعادة بناء متعددة اللغات وقابلة للبحث لأرشيف [PURSUE UAP archive](https://www.war.gov/ufo)). التعليقات / الانتقادات مرحب بها.

### 1. الـ pipeline هو state machine ذات اتجاه واحد للأمام — عن قصد

الحالات: `discovered → downloaded → ocr_done → translated → published`. الوثيقة كتمشي غير لقدام، و غير فاش كتكون شي خدمة خاصها تدار. المحتوى المنشور عمرو ما كيتعاود يتعالج إلا إذا شاف واحد الكاشف ديال الفروقات أن المصدر تبدل بصح.

**علاش:** الـ OCR + الترجمة هوما العمليات الغالية، والأرشيف كيكبر مع الوقت. pipeline لي "كيعاود كلشي باش يتأكد" عندو تكلفة غير محدودة. جعل الانتقالات للخلف مستحيلة كيخلي فاتورة هاربة مستحيلة. سقف التكلفة هو خاصية من خصائص الرسم البياني للحالة، ماشي من يقظة المشغل.

**التكلفة:** عمليات ترحيل الـ schema وإعادة المعالجة عن قصد صعييبة شوية. مقايضة مقبولة.

### 2. الـ OCR و الترجمة خدامين على LLM محلي، ماشي على API ديال الـ cloud

OCR: محرك مفتوح المصدر، مع Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على لابتوب Apple Silicon.

**علاش:** تكلفة هامشية صفرية لكل وثيقة؛ قابلة للتكرار (موديل و prompts ثابتين)؛ و الخطوة ديال الجلب أصلا خاصها تدار من IP سكني (المصدر محمي بـ Akamai Bot Manager — `curl` كتجيه 403)، إذن اللابتوب داخل فالحلقة كيفما كان الحال.

**التكلفة:** جودة الترجمة أقل من موديل متطور. بالنسبة ل корпуوس مرجعي فين الإنجليزية الأصلية ديما على بعد نقرة وحدة، هادشي مقبول. حنا ما كندعيوش أن الترجمات موثوقة.

### 3. النصفيْن بجوج كيتشاركو ف واجهة وحدة بالضبط: حزمة منشورة

الـ pipeline عمرو ما كيكتب مباشرة فقاعدة البيانات ديال الإنتاج. هو كيخرج `{ SQL, asset manifest, cache-purge list }`. "النشر" = تطبيق ديك الحزمة لقدام (دفع SQL لقاعدة بيانات SQL فالـ edge، مزامنة الـ assets مع object storage، مسح مفاتيح الـ cache المحددة).

**علاش:** الجانب المحلي و جانب الـ edge يقدرو يتطورو بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" عندو نفس الشكل كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — CSP صارم (ماكايناش `unsafe-inline`؛ JSON-LD المضمن كيتثبت بـ sha256)، تفاوض `Accept-Language` + بلد→لغة، cache ديال الصفحات ف KV لمدة 30 يوم، cron يومي للصيانة — و عمرو ما كيحتاج يعرف كيفاش تصاوبات البيانات.

**التكلفة:** تغيير D1 schema كيخص جوج ملفات (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`). تأمين رخيص.

### أمور غير قابلة للتفاوض مضمنة ف السلوك

- ما عندو علاقة بالحكومة الأمريكية؛ ماكاين حتى شارة رسمية.
- التنقيحات الأصلية محفوظة، عمرو ما كتعكس.
- الفيديو منسوب لـ DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` فالموقع كامل — قابل للفهرسة، ومختار عدم المشاركة فالكشط من طرف الذكاء الاصطناعي.

خدام دابا: https://www.ufolens.com · API: https://www.ufolens.com/api/v1
