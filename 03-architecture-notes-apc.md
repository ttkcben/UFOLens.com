# GitHub — منشور 3 من 3 · ملاحظات هيكلية (مناقشة بأسلوب ADR)

**يُستخدم كـ:** مناقشة تحت "Show and tell" / "Architecture"، أو كنقطة بداية لـ ADR في `docs/`.
**كلمات مفتاحية:** architecture, ADR, forward-only state machine, local LLM, Ollama, OCR, edge computing, CSP, security headers, data pipeline, cost engineering, SQLite manifest, D1, R2, KV
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## ليش ufolens.com مبني بهالطريقة

ملاحظات على القرارات التلاتة اللي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث ومتعددة اللغات لـ [أرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات / الانتقادات.

### 1. خط الأنابيب هو آلة حالة ذات مسار أمامي فقط — عن قصد

الحالات: `discovered → downloaded → ocr_done → translated → published`. الوثيقة بتمشي لقدام بس، وبس لما يكون في شغل لازم ينعمل. المحتوى المنشور ما بيتعالج مرة تانية إلا إذا كاشف التغييرات شاف إنه المصدر تغير فعلاً.

**ليش:** OCR + الترجمة هن العمليات المكلفة، والأرشيف بيكبر مع الوقت. خط الأنابيب اللي "بيعيد تشغيل كل شي ليكون آمن" تكلفته غير محدودة. جعل الانتقالات للخلف مستحيلة بيجعل الفاتورة الهاربة مستحيلة. سقف التكلفة هو خاصية من الرسم البياني للحالة، مش من يقظة المشغل.

**التكلفة:** ترحيل المخططات وإعادة المعالجة عن قصد بتكون صعبة شوي. هي مبادلة مقبولة.

### 2. OCR والترجمة بيشتغلوا على LLM محلي، مش API سحابي

OCR: محرك مفتوح المصدر، مع Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على لابتوب Apple Silicon.

**ليش:** تكلفة هامشية صفر لكل وثيقة؛ قابل لإعادة الإنتاج (موديل + prompts ثابتين)؛ ومرحلة الجلب (fetch) أصلاً لازم تشتغل من IP سكني (المصدر وراه Akamai Bot Manager — `curl` بيحصل على 403)، فاللابتوب موجود بالحلقة بكل الأحوال.

**التكلفة:** جودة الترجمة أقل من الموديلات الرائدة. بالنسبة لمرجع أرشيفي بيكون فيه النص الإنجليزي الأصلي دايمًا على بعد نقرة واحدة، هالشي مقبول. إحنا ما بندعي إنه الترجمات موثوقة.

### 3. النصين بيشتركوا بواجهة واحدة بالضبط: حزمة منشورة

خط الأنابيب ما بيكتب أبداً على قاعدة بيانات الإنتاج مباشرة. هو بينتج `{ SQL, بيان أصول, قائمة مسح ذاكرة التخزين المؤقت }`. "النشر" = تطبيق هي الحزمة للأمام (دفع SQL لقاعدة بيانات SQL على الحافة، مزامنة الأصول مع تخزين الكائنات، مسح مفاتيح ذاكرة التخزين المؤقت المحددة).

**ليش:** الجانب المحلي وجانب الحافة ممكن يتطوروا بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" إله نفس الشكل كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — CSP صارم (ما في `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض لغة عبر `Accept-Language` + بلد→لغة، ذاكرة تخزين مؤقت للصفحات بـ KV لمدة 30 يومًا، cron صيانة يومي — وما بيحتاج أبداً يعرف كيف البيانات انعملت.

**التكلفة:** تغيير مخطط D1 بيأثر على ملفين (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`). تأمين رخيص.

### أمور غير قابلة للتفاوض مبنية في السلوك

- غير تابع لحكومة الولايات المتحدة؛ ما في شعارات رسمية.
- الأجزاء المحجوبة من المصدر بتضل محفوظة، وما بتنعكس أبداً.
- الفيديو منسوب لـ DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من قبل محركات البحث، ومستبعد من كشط الذكاء الاصطناعي.

الموقع: https://www.ufolens.com · API: https://www.ufolens.com/api/v1

