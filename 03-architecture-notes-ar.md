# GitHub — المنشور 3 من 3 · ملاحظات حول البنية المعمارية (نقاش بأسلوب ADR)

**الاستخدام:** كنقاش تحت قسم "Show and tell" / "Architecture"، أو كبذرة `docs/` ADR.
**الكلمات المفتاحية:** architecture، ADR، forward-only state machine، local LLM، Ollama، OCR، edge computing، CSP، security headers، data pipeline، cost engineering، SQLite manifest، D1، R2، KV
**الروابط التشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## لماذا تم بناء ufolens.com بهذه الطريقة

ملاحظات حول القرارات الثلاثة التي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث ومتعددة اللغات لـ [PURSUE UAP archive](https://www.war.gov/ufo)). نرحب بالتعليقات والملاحظات.

### 1. خط معالجة البيانات (pipeline) هو forward-only state machine — عن قصد

الحالات: `discovered → downloaded → ocr_done → translated → published`. ينتقل المستند للأمام فقط، وفقط عندما يكون هناك عمل يجب القيام به. لا يتم إعادة معالجة المحتوى المنشور أبداً إلا إذا رصد كاشف الفروقات (delta detector) تغييراً فعلياً في المصدر.

**السبب:** عمليات OCR والترجمة هي العمليات المكلفة، والأرشيف ينمو بمرور الوقت. خط المعالجة الذي "يعيد تشغيل كل شيء لضمان السلامة" يتسبب في تكلفة غير محدودة. جعل الانتقالات العكسية مستحيلة يجعل من المستحيل حدوث تضخم غير متوقع في الفاتورة. سقف التكلفة هو خاصية من خصائص مخطط الحالة (state graph)، وليس نتيجة ليقظة المشغل.

**التكلفة:** عمليات ترحيل المخطط (schema migrations) وإعادة المعالجة المتعمدة تكون صعبة ومزعجة بشكل مقصود. وهي مقايضة مقبولة.

### 2. تعمل OCR والترجمة على نموذج لغوي كبير محلي (local LLM)، وليس API سحابي

OCR: محرك مفتوح المصدر، مع خيار بديل Tesseract CLI. الترجمة + NER: Gemma عبر Ollama، على جهاز كمبيوتر محمول Apple Silicon.

**السبب:** تكلفة هامشية صفرية لكل مستند؛ قابلية التكرار (نموذج ثابت + مطالبات/prompts)؛ كما أن خطوة الجلب (fetch) يجب أن تعمل بالفعل من عنوان IP منزلي (المصدر موجود خلف Akamai Bot Manager — و `curl` يتلقى خطأ 403)، لذا فإن استخدام الكمبيوتر المحمول ضروري في كل الأحوال.

**التكلفة:** جودة الترجمة أقل من النماذج الرائدة (frontier models). بالنسبة لمجموعة مرجعية حيث تكون النسخة الإنجليزية الأصلية دائماً على بعد نقرة واحدة، فإن هذا أمر مقبول. نحن لا ندعي أن الترجمات هي المرجع النهائي.

### 3. يتشارك النصفان واجهة واحدة فقط: حزمة منشورة (published bundle)

لا يقوم خط المعالجة بالكتابة مباشرة في قاعدة بيانات الإنتاج. بل يقوم بإصدار `{ SQL, asset manifest, cache-purge list }`. "النشر" = تطبيق تلك الحزمة للأمام (دفع SQL إلى قاعدة بيانات edge SQL، ومزامنة الأصول إلى object storage، وتطهير مفاتيح التخزين المؤقت المحددة).

**السبب:** يمكن للجانب المحلي وجانب الـ edge أن يتطورا بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و"بيانات النشر" لها نفس الهيكل في كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — يتبع CSP صارم (بدون `unsafe-inline`؛ الـ JSON-LD المضمن مثبت على إصدار sha256)، مع `Accept-Language` + تفاوض الدولة→اللغة، وتخزين مؤقت لصفحات KV لمدة 30 يوماً، ومهام صيانة يومية (cron) —، ولا يحتاج أبداً لمعرفة كيفية إنشاء البيانات.

**التكلفة:** أي تغيير في مخطط D1 يؤثر على ملفين (`pipeline/lib/manifest_schema.sql`، `db/schema.sql`). وهو تأمين منخفض التكلفة.

### ثوابت غير قابلة للتفاوض مدمجة في السلوك

- غير تابع للحكومة الأمريكية؛ لا توجد شارات رسمية.
- يتم الحفاظ على عمليات التظليل/الحذف في المصدر، ولا يتم عكسها أبداً.
- يتم إرجاع الفيديوهات إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابلة للفهرسة في البحث، مع رفض الكشط بواسطة الذكاء الاصطناعي (AI-scrape-opted-out).

مباشر: https://www.ufolens.com · API: https://www.ufolens.com/api/v1