# GitHub — المنشور 2 من 3 · دعوة للمساهمين / "مشاكل أولى جيدة"

**يُستخدم كـ:** مناقشة مثبتة ("المساهمة ومشاكل أولى جيدة") أو مقدمة لـ CONTRIBUTING.md.
**الكلمات المفتاحية:** مفتوح المصدر، مساهمة، مشكلة أولى جيدة، i18n، توطين، OCR، Python، TypeScript، Vitest، pytest، إمكانية الوصول، UAP، بيانات مفتوحة
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة في ufolens.com

[ufolens.com](https://www.ufolens.com) يحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) التابع لوزارة الحرب الأمريكية إلى منصة قابلة للبحث ومتعددة اللغات مع [API عام](https://www.ufolens.com/api/v1). يتكون من نصفين — خط أنابيب استيعاب محلي بلغة Python (`pipeline/`) وتطبيق حافة بلغة TypeScript/Hono (`worker/`) — يلتقيان عند واجهة واحدة: حزمة SQL + أصول منشورة.

لا تحتاج إلى أي بيانات اعتماد سحابية للمساهمة. الوحدات الأساسية لخط الأنابيب هي stdlib-only فقط واختبارات العامل (Worker) تعمل مقابل تخزين في الذاكرة.

### الإعداد

```bash
# pipeline
python3 -m pytest pipeline/tests/          # يجب أن تكون جميعها خضراء، لا حاجة لـ pip install

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### أين تكون المساعدة أكثر فائدة

**i18n / التوطين** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة المتحدثين الأصليين لأي لغة غير إنجليزية تعتبر ذات قيمة عالية: اكتشاف الترجمات الآلية الركيكة، إصلاح مشاكل RTL/التخطيط، تحسين الحالات الهامشية لتفاوض اللغة.

**جودة OCR** — معالجة مسبقة أفضل للمستندات المطبوعة بالآلة الكاتبة القديمة قبل OCR؛ إطار تقييم يقارن بين المحرك مفتوح المصدر والبديل Tesseract على صفحات عينة.

**إمكانية الوصول** — تدقيق الصفحات المعروضة (`worker/src/render/`) مقابل معايير WCAG؛ إن CSP صارم (لا يوجد `unsafe-inline`)، لذا يجب أن تعمل الحلول ضمن هذا الإطار.

**بيئة عمل API** — `worker/src/routes/` — التقسيم إلى صفحات، التصفية، وصف OpenAPI، عملاء أمثلة.

**متانة خط الأنابيب** — مسارات تدهور أكثر رشاقة، تقارير تقدم أفضل، حالات هامشية لكشف الفروق (`pipeline/lib/delta.py`).

**الوثائق** — `docs/20260511/` (繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة وثائق التصميم إلى الإنجليزية.

### القواعد الأساسية

- جميع المسارات نسبية — يجب أن يكون المشروع قابلاً للنقل بين الأجهزة. لا توجد مسارات مطلقة مبرمجة بشكل ثابت.
- لا تضف تبعية pip إلى وحدة *أساسية* في خط الأنابيب. قد تستخدم المراحل الاختيارية حزمًا اختيارية، ويجب أن تتدهور برشاقة بدونها.
- لا تضعف آلة الحالة التقدمية فقط — فهذا هو سقف التكلفة.
- لا تقدم شارات حكومية أمريكية رسمية، ولا تضف أي شيء يعكس التنقيحات الأصلية.
- تغييرات مخطط D1 تلمس ملفين **اثنين**: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- اختبارات مع الكود الجديد. رسائل commit تقليدية (Conventional-commit).

اقرأ `CLAUDE.md` و `docs/20260511/00-*` أولاً، ثم افتح مشكلة لمناقشة أي شيء هيكلي قبل تقديم طلب السحب (PR).
