# GitHub — Δημοσίευση 2 από 3 · Πρόσκληση για συνεισφορά / "καλά πρώτα ζητήματα"

**Χρήση ως:** μια καρφιτσωμένη Συζήτηση ("Συνεισφορά & καλά πρώτα ζητήματα") ή ως εισαγωγή στο CONTRIBUTING.md.
**Λέξεις-κλειδιά:** ανοιχτός κώδικας, συνεισφορά, καλό πρώτο ζήτημα, i18n, τοπικοποίηση, OCR, Python, TypeScript, Vitest, pytest, προσβασιμότητα, UAP, ανοιχτά δεδομένα
**Υπερσύνδεσμοι:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## Συνεισφορά στο ufolens.com

Το [ufolens.com](https://www.ufolens.com) μετατρέπει το [αρχείο PURSUE UAP](https://www.war.gov/ufo) του Πολεμικού Υπουργείου των ΗΠΑ σε μια αναζητήσιμη, πολυγλωσσική πλατφόρμα με ένα [δημόσιο API](https://www.ufolens.com/api/v1). Αποτελείται από δύο μισά — ένα τοπικό pipeline εισαγωγής σε Python (`pipeline/`) και μια edge εφαρμογή σε TypeScript/Hono (`worker/`) — που συναντιούνται σε μία διεπαφή: ένα δημοσιευμένο πακέτο SQL + assets.

Δεν χρειάζεστε διαπιστευτήρια cloud για να συνεισφέρετε. Οι βασικές ενότητες του pipeline είναι μόνο stdlib και οι δοκιμές του Worker εκτελούνται έναντι αποθήκευσης στη μνήμη.

### Εγκατάσταση

```bash
# pipeline
python3 -m pytest pipeline/tests/          # θα πρέπει να είναι όλα πράσινα, δεν χρειάζεται pip install

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### Πού είναι πιο χρήσιμη η βοήθεια

**i18n / τοπικοποίηση** — Το `worker/src/i18n/ui-strings.json` είναι η πηγή των αλφαριθμητικών του UI. Η αξιολόγηση από φυσικούς ομιλητές οποιασδήποτε μη αγγλικής γλώσσας είναι υψηλής αξίας: εντοπισμός αμήχανων μηχανικών μεταφράσεων, διόρθωση ζητημάτων RTL/διάταξης, βελτίωση οριακών περιπτώσεων διαπραγμάτευσης γλώσσας.

**Ποιότητα OCR** — καλύτερη προεπεξεργασία παλαιών δακτυλογραφημένων σαρώσεων πριν από το OCR· πλαίσιο αξιολόγησης που συγκρίνει τη μηχανή ανοιχτού κώδικα με την εναλλακτική Tesseract σε δείγματα σελίδων.

**Προσβασιμότητα** — έλεγχος των παραγόμενων σελίδων (`worker/src/render/`) έναντι του WCAG· το CSP είναι αυστηρό (όχι `unsafe-inline`), επομένως οι λύσεις πρέπει να λειτουργούν εντός αυτού του πλαισίου.

**Εργονομία API** — `worker/src/routes/` — σελιδοποίηση, φιλτράρισμα, περιγραφή OpenAPI, παραδείγματα πελατών.

**Ευστάθεια του Pipeline** — περισσότερες διαδρομές ομαλής υποβάθμισης, καλύτερη αναφορά προόδου, οριακές περιπτώσεις ανίχνευσης δέλτα (`pipeline/lib/delta.py`).

**Έγγραφα** — `docs/20260511/` (繁體中文· το `00-*` είναι ο δείκτης). Μεταφράσεις των εγγράφων σχεδιασμού στα Αγγλικά είναι ευπρόσδεκτες.

### Βασικοί κανόνες

- Όλες οι διαδρομές σχετικές — το έργο πρέπει να είναι φορητό μεταξύ μηχανημάτων. Όχι απόλυτες διαδρομές κωδικοποιημένες απευθείας.
- Μην προσθέτετε εξάρτηση pip σε μια *βασική* ενότητα του pipeline. Προαιρετικά στάδια μπορούν να χρησιμοποιούν προαιρετικά πακέτα και πρέπει να υποβαθμίζονται ομαλά χωρίς αυτά.
- Μην αποδυναμώνετε τη μηχανή καταστάσεων μόνο προς τα εμπρός — αυτό είναι το ανώτατο όριο κόστους.
- Μην εισάγετε επίσημα διακριτικά της κυβέρνησης των ΗΠΑ και μην προσθέτετε τίποτα που αναιρεί τις διαγραφές της πηγής.
- Οι αλλαγές στο σχήμα D1 επηρεάζουν **δύο** αρχεία: `pipeline/lib/manifest_schema.sql` και `db/schema.sql`.
- Δοκιμές με νέο κώδικα. Μηνύματα commit τύπου Conventional-commit.

Διαβάστε πρώτα το `CLAUDE.md` και το `docs/20260511/00-*`, και μετά ανοίξτε ένα ζήτημα για να συζητήσετε οτιδήποτε δομικό πριν από το PR.

