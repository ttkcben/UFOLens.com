# GitHub — منشور 2 من 3 · دعوة للمساهمين / "مهام أولى جيدة"

**يُستخدم كـ:** مناقشة مثبتة ("المساهمة ومهام أولى جيدة") أو مقدمة لملف CONTRIBUTING.md.
**كلمات مفتاحية:** مفتوح المصدر, مساهمة, مهمة أولى جيدة, i18n, توطين, OCR, Python, TypeScript, Vitest, pytest, إمكانية الوصول, UAP, بيانات مفتوحة
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## المساهمة في ufolens.com

[ufolens.com](https://www.ufolens.com) يحول [أرشيف PURSUE UAP](https://www.war.gov/ufo) التابع لوزارة الحرب الأمريكية إلى منصة متعددة اللغات وقابلة للبحث مع [API عام](https://www.ufolens.com/api/v1). يتكون المشروع من نصفين — pipeline استيعاب محلي بلغة Python (`pipeline/`) وتطبيق طرفي بـ TypeScript/Hono (`worker/`) — يلتقيان عند واجهة واحدة: حزمة SQL + أصول منشورة.

لا تحتاج إلى أي بيانات اعتماد سحابية للمساهمة. الوحدات الأساسية للـ pipeline لا تتطلب مكتبات خارجية، واختبارات الـ Worker تعمل مقابل تخزين في الذاكرة.

### الإعداد

```bash
# pipeline
python3 -m pytest pipeline/tests/          # يجب أن تكون جميع الاختبارات ناجحة، لا حاجة لـ pip install

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### أكثر الجوانب التي نحتاج فيها للمساعدة

**i18n / التوطين** — `worker/src/i18n/ui-strings.json` هو مصدر نصوص واجهة المستخدم. مراجعة أي لغة غير الإنجليزية من قبل متحدثين أصليين لها قيمة عالية: اكتشاف الترجمات الآلية الركيكة، إصلاح مشاكل RTL/التخطيط، تحسين الحالات الهامشية لتفاوض اللغة.

**جودة OCR** — معالجة مسبقة أفضل للمستندات الممسوحة ضوئيًا القديمة والمكتوبة بالآلة الكاتبة قبل الـ OCR؛ بناء إطار تقييم يقارن بين المحرك مفتوح المصدر والبديل Tesseract على صفحات عينة.

**إمكانية الوصول** — تدقيق الصفحات المعروضة (`worker/src/render/`) وفقًا لـ WCAG؛ الـ CSP صارم (بدون `unsafe-inline`)، لذلك يجب أن تعمل الحلول ضمن هذا الإطار.

**سهولة استخدام الـ API** — `worker/src/routes/` — تقسيم الصفحات، الفلترة، وصف OpenAPI، عملاء مثال.

**متانة الـ pipeline** — مسارات أكثر للمرونة في حالة الفشل، إبلاغ أفضل عن التقدم، حالات هامشية في كشف التغييرات (`pipeline/lib/delta.py`).

**الوثائق** — `docs/20260511/` (بالصينية التقليدية 繁體中文؛ `00-*` هو الفهرس). نرحب بترجمة وثائق التصميم إلى اللغة الإنجليزية.

### القواعد الأساسية

- كل المسارات نسبية — يجب أن يكون المشروع قابلًا للنقل بين الأجهزة. لا مسارات مطلقة مكتوبة بشكل ثابت.
- لا تضف تبعية pip إلى وحدة *أساسية* في الـ pipeline. قد تستخدم المراحل الاختيارية حزمًا اختيارية، ويجب أن تعمل بشكل أقل كفاءة ولكن لا تتوقف عند غيابها.
- لا تضعف آلة الحالات ذات الاتجاه الأمامي فقط — فهذا هو سقف التكلفة.
- لا تضف أي شعارات رسمية لحكومة الولايات المتحدة، ولا تضف أي شيء يلغي التنقيحات في المصدر.
- تغييرات مخطط D1 تلمس ملفين **اثنين**: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- الاختبارات مع الكود الجديد. رسائل commit تقليدية (Conventional-commit).

اقرأ `CLAUDE.md` و `docs/20260511/00-*` أولاً، ثم افتح "issue" لمناقشة أي شيء هيكلي قبل إرسال الـ PR.

