# GitHub — פוסט 3 מתוך 3 · הערות ארכיטקטורה (בסגנון ADR)

**שימוש:** דיון תחת "הצגה וסיפור" / "ארכיטקטורה", או כבסיס למסמך ADR בתיקיית `docs/`.
**מילות מפתח:** ארכיטקטורה, ADR, מכונת מצבים קדימה-בלבד, LLM מקומי, Ollama, OCR, מחשוב קצה, CSP, כותרות אבטחה, צינור נתונים, הנדסת עלויות, מניפסט SQLite, D1, R2, KV
**היפר-קישורים:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## מדוע ufolens.com בנוי כפי שהוא בנוי

הערות על שלוש ההחלטות שעיצבו את [ufolens.com](https://www.ufolens.com) (השחזור הרב-לשוני והניתן לחיפוש של [ארכיון ה-PURSUE UAP](https://www.war.gov/ufo)). נשמח לקבל הערות / התנגדויות.

### 1. ה-pipeline הוא מכונת מצבים המתקדמת-קדימה-בלבד (forward-only) — ובכוונה תחילה

מצבים: `discovered → downloaded → ocr_done → translated → published`. מסמך נע רק קדימה, ורק כשיש עבודה לבצע. תוכן שפורסם לעולם לא מעובד מחדש, אלא אם גלאי דלתא (delta detector) מזהה שהמקור אכן השתנה.

**הסיבה:** OCR + תרגום הן הפעולות היקרות, והארכיון גדל עם הזמן. ל-pipeline ש"מריץ הכל מחדש כדי להיות בטוח" יש עלות בלתי-חסומה. הפיכת מעברים לאחור לבלתי אפשריים הופכת חשבון שיוצא משליטה לבלתי אפשרי. תקרת העלות היא תכונה של גרף המצבים, לא של עירנות המפעיל.

**המחיר:** העברות סכמה (schema migrations) ועיבוד מחדש-בכוונה הם מסורבלים במכוון. זוהי פשרה מקובלת.

### 2. OCR ותרגום רצים על LLM מקומי, לא על API בענן

OCR: מנוע קוד פתוח, עם Tesseract CLI כחלופה. תרגום + NER: Gemma דרך Ollama, על מחשב נייד של Apple Silicon.

**הסיבה:** עלות שולית אפסית לכל מסמך; ניתן לשחזור (reproducible) (מודל קבוע + הנחיות קבועות); ושלב ה-fetch כבר חייב לרוץ מכתובת IP ביתית (המקור נמצא מאחורי Akamai Bot Manager — הפקודה `curl` מקבלת 403), כך שמחשב נייד כבר נמצא במעגל בכל מקרה.

**המחיר:** איכות התרגום נמוכה מזו של מודל קצה (frontier model). עבור מאגר ייחוס (reference corpus) שבו המקור באנגלית תמיד נמצא במרחק קליק אחד, זה בסדר גמור. איננו טוענים שהתרגומים הם מוסמכים.

### 3. שני החצאים חולקים בדיוק ממשק אחד: חבילה מפורסמת (published bundle)

ה-pipeline לעולם אינו כותב ישירות למסד הנתונים של סביבת הייצור. הוא פולט `{ SQL, מניפסט נכסים, רשימת מחיקת מטמון }`. "פרסום" = החלת החבילה הזו קדימה (דחיפת SQL למסד הנתונים SQL בקצה, סנכרון נכסים לאחסון האובייקטים, מחיקת מפתחות המטמון שצוינו).

**הסיבה:** הצד המקומי וצד הקצה (edge) יכולים להתפתח באופן עצמאי; החבילה ניתנת לסקירה; ו"פריסת נתונים" (deploy data) היא בעלת אותה צורה בכל פעם. ה-Worker הוא אפליקציית TypeScript/Hono קטנה — CSP מחמיר (ללא `unsafe-inline`; קוד JSON-LD מוטבע מאומת באמצעות sha256), משא ומתן `Accept-Language` + מדינה→שפה, מטמון עמודים ב-KV ל-30 יום, cron תחזוקה יומי — והוא לעולם לא צריך לדעת כיצד הנתונים נוצרו.

**המחיר:** שינוי סכמה ב-D1 נוגע בשני קבצים (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`). ביטוח זול.

### עקרונות בלתי-ניתנים-למשא-ומתן הטבועים בהתנהגות המערכת

- אינו משויך לממשלת ארה"ב; אין סמלים רשמיים.
- צנזורה (redactions) מהמקור נשמרת, ולעולם לא מבוטלת.
- וידאו מיוחס ל-DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` בכל האתר — ניתן לאינדוקס-חיפוש, עם התנגדות לגירוד-נתונים-עבור-AI.

קישור לאתר: https://www.ufolens.com · API: https://www.ufolens.com/api/v1
