# GitHub — منشور 1 من 3 · إعلان الإصدار / بلوك README

**يُستخدم كـ:** نص أساسى لإصدار على GitHub، أو مناقشة مثبتة، أو فى أول ملف README للريبو.
**الكلمات المفتاحية:** UAP، UFO، أرشيف PURSUE، وثائق رفعت سريتها، بيانات مفتوحة، بحث فى النص الكامل، OCR، ترجمة آلية، LLM محلى، Ollama، حوسبة الحافة، API عام، Hono، TypeScript، Python
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — منصة متعددة اللغات وقابلة للبحث لأرشيف PURSUE UAP

**الموقع المباشر:** https://www.ufolens.com · **الـ API:** https://www.ufolens.com/api/v1 · **الأرشيف المصدر:** https://www.war.gov/ufo

`ufolens.com` بيعيد نشر أرشيف **PURSUE** الخاص بوزارة الحرب الأمريكية واللى فيه سجلات UAP / UFO اللى ارفعت عنها السرية، كمنصة معرفية: بحث كامل فى النصوص، ترجمة آلية على مستوى الأرشيف كله، استكشاف بالخريطة والخط الزمنى، و API عام بصيغة JSON. المستندات المصدرية هى من أعمال حكومة الولايات المتحدة الفيدرالية، وبالتالى هى فى الملكية العامة داخل الولايات المتحدة ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). المشروع ده **مش تبع حكومة الولايات المتحدة**، ومبيستخدمش أى شارات رسمية، ومبيحاولش أبداً يكشف عن المعلومات المحجوبة.

### الهيكل العام (Architecture)

```
جهاز محلى (Apple Silicon، عنوان IP منزلى)          شبكة الحافة (Edge network)
─────────────────────────────────────────           ─────────────────────────
pipeline/  (Python 3.10، القلب يعتمد على المكتبة القياسية فقط)   worker/  (TypeScript, Hono.js)
  جلب → OCR → ترجمة → نشر (اتجاه واحد للأمام فقط)      /{lang}/...   صفحات
  الـ OCR: محرك مفتوح المصدر (مع الاعتماد على Tesseract CLI كبديل)   /api/v1/...   API عام
  الترجمة / NER: نموذج لغوى محلى (Gemma عن طريق Ollama)        /admin        لوحة تحكم المشغل
  الحالة: بيان SQLite                              مدعوم من: قاعدة بيانات SQL على الحافة، تخزين
        │                                              كائنات (ملفات PDF المصدر)، ذاكرة تخزين مؤقت KV
        └─ ينشر حزمة: SQL + بيان الأصول + قائمة بملفات الكاش المطلوب حذفها ─┘
```

- **تكلفة صفرية لكل مستند بالنسبة للذكاء الاصطناعى السحابى.** عمليات الـ OCR والترجمة بتشتغل محلياً؛ وآلية الحالة ذات الاتجاه الواحد للأمام (`مكتشف ← محمل ← تم عمل ocr ← مترجم ← منشور`) بتضمن عدم إعادة معالجة أى مستند إلا لو اتغير.
- **قلب الـ pipeline معلوش أى اعتماديات على أطراف تالتة** — وحدات التحليل / البيان / التغييرات بتشتغل وبتتختبر على Python نضيف من غير أى حاجة متثبتة بـ pip؛ مراحل الـ OCR/الترجمة بتتدهور برشاقة (degrade gracefully) لما الحزم الاختيارية تكون مش موجودة.
- **موقع الحافة** بيطبق ترويسات أمان صارمة + CSP (مفيش `unsafe-inline`؛ الـ JSON-LD المضمن مثبت بـ sha256)، والتفاوض على اللغة بيتم عن طريق `Accept-Language` + ربط الدول، وذاكرة تخزين مؤقت للصفحات فى KV لمدة 30 يوم، و cron يومى للصيانة.
- **تحديثات تزايدية:** كاشف التغييرات (delta detector) بيقارن فهرس المصدر وبيدخل التغييرات بس للـ pipeline.

### للمطورين

الـ API العام على https://www.ufolens.com/api/v1 بيرجع المستندات والبيانات الوصفية بصيغة JSON. الوصول المجهول محدود بمعدل معين (rate-limited)؛ اطلب مفتاح للوصول لمستويات الباحثين/المطورين. شوف قسم ה-API على الموقع عشان تعرف نقاط النهاية (endpoints) والحدود.

### الحالة

الكود مكتمل؛ الموقع منشور على https://www.ufolens.com. قاعدة بيانات الإنتاج بيتم ملؤها عن طريق تشغيل الـ pipeline اللى مش متصل بالإنترنت ونشر الحزمة للأمام (`cli_publish run --remote`). مستندات التصميم الكاملة موجودة فى `docs/20260511/`.

### الترخيص / الحدود

- المستندات المصدرية: من أعمال حكومة الولايات المتحدة الفيدرالية، فى الملكية العامة داخل الولايات المتحدة.
- الكود الخاص بالمنصة دى: شوف `LICENSE`.
- الموقع بيرسل `Tdm-Reservation: 1` و `X-Robots-Tag: noai, noimageai` — قابل للفهرسة من محركات البحث، لكنه معطل خاصية التدريب/الكشط من نماذج الذكاء الاصطناعى.
- مقاطع الفيديو منسوبة لـ DVIDS / AARO والمشروع ده لا يدعى ملكيتها.

نرحب بالـ Issues والـ PRs. برجاء قراءة `CLAUDE.md` و `docs/20260511/00-*` قبل فتح أى تغييرات هيكلية.

