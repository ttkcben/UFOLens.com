# GitHub — پست ۲ از ۳ · فراخوان مشارکت / "مسائل خوب برای شروع"

**استفاده به عنوان:** یک Discussion پین‌شده ("مشارکت و مسائل خوب برای شروع") یا مقدمه CONTRIBUTING.md.
**کلمات کلیدی:** منبع باز، مشارکت، مسئله خوب برای شروع، i18n، محلی‌سازی، OCR، Python، TypeScript، Vitest، pytest، دسترسی‌پذیری، UAP، داده‌های باز
**لینک‌ها:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## مشارکت در ufolens.com

[ufolens.com](https://www.ufolens.com) آرشیو [PURSUE UAP](https://www.war.gov/ufo) وزارت جنگ ایالات متحده را به یک پلتفرم قابل جستجو و چندزبانه با یک [API عمومی](https://www.ufolens.com/api/v1) تبدیل می‌کند. این پروژه از دو نیمه تشکیل شده است — یک پایپ‌لاین دریافت محلی با Python (`pipeline/`) و یک اپلیکیشن لبه با TypeScript/Hono (`worker/`) — که در یک رابط با هم تلاقی می‌کنند: یک بسته منتشر شده از SQL + دارایی‌ها.

برای مشارکت نیازی به هیچ اعتبار ابری ندارید. ماژول‌های اصلی پایپ‌لاین فقط از کتابخانه استاندارد استفاده می‌کنند و تست‌های Worker در برابر حافظه درون‌برنامه‌ای اجرا می‌شوند.

### راه‌اندازی

```bash
# pipeline
python3 -m pytest pipeline/tests/          # باید همگی سبز باشند، نیازی به نصب pip نیست

# worker
cd worker && npm install
npm run typecheck && npm test              # tsc --noEmit + vitest
npm run dev                                # localhost:8787
```

### جایی که کمک بیشترین فایده را دارد

**i18n / محلی‌سازی** — `worker/src/i18n/ui-strings.json` منبع رشته‌های UI است. بازبینی توسط یک گوینده بومی برای هر زبان غیرانگلیسی ارزش بالایی دارد: شناسایی خروجی ماشینی نامناسب، رفع مشکلات RTL/چیدمان، بهبود موارد خاص در مذاکره زبان.

**کیفیت OCR** — پیش‌پردازش بهتر اسکن‌های تایپ‌شده قدیمی قبل از OCR؛ ابزار ارزیابی برای مقایسه موتور منبع باز با جایگزین Tesseract روی صفحات نمونه.

**دسترسی‌پذیری** — بررسی صفحات رندر شده (`worker/src/render/`) در برابر WCAG؛ CSP سختگیرانه است (بدون `unsafe-inline`)، بنابراین راه‌حل‌ها باید در این چارچوب کار کنند.

**ارگونومی API** — `worker/src/routes/` — صفحه‌بندی، فیلتر کردن، توضیحات OpenAPI، کلاینت‌های نمونه.

**استحکام پایپ‌لاین** — مسیرهای تنزل آرام بیشتر، گزارش‌دهی پیشرفت بهتر، موارد خاص در تشخیص دلتا (`pipeline/lib/delta.py`).

**مستندات** — `docs/20260511/` (繁體中文؛ `00-*` شاخص است). از ترجمه مستندات طراحی به انگلیسی استقبال می‌شود.

### قوانین اصلی

- همه مسیرها نسبی هستند — پروژه باید قابل حمل بین ماشین‌ها باشد. هیچ مسیر مطلق کدگذاری‌شده‌ای نباید وجود داشته باشد.
- یک وابستگی pip به ماژول *اصلی* پایپ‌لاین اضافه نکنید. مراحل اختیاری ممکن است از بسته‌های اختیاری استفاده کنند و باید بدون آن‌ها به آرامی تنزل یابند.
- ماشین وضعیت فقط رو به جلو را تضعیف نکنید — این سقف هزینه است.
- نشان‌های رسمی دولت ایالات متحده را اضافه نکنید و چیزی که ویرایش‌های منبع را برگرداند اضافه نکنید.
- تغییرات شمای D1 **دو** فایل را تحت تأثیر قرار می‌دهد: `pipeline/lib/manifest_schema.sql` و `db/schema.sql`.
- تست‌ها با کد جدید. پیام‌های کامیت به سبک Conventional-commit.

ابتدا `CLAUDE.md` و `docs/20260511/00-*` را بخوانید، سپس برای بحث در مورد هرگونه تغییر ساختاری قبل از ارسال PR، یک issue باز کنید.

