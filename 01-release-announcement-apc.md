# GitHub — منشور 1 من 3 · إعلان الإصدار / قسم README

**يُستخدم كـ:** نص أساسي لإصدار على GitHub، أو مناقشة مثبتة، أو في أعلى ملف README الخاص بالمستودع.
**كلمات مفتاحية:** UAP, UFO, PURSUE archive, declassified documents, open data, full-text search, OCR, machine translation, local LLM, Ollama, edge computing, public API, Hono, TypeScript, Python
**روابط:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo · https://www.copyright.gov/title17/92chap1.html#105

---

## ufolens.com — منصة متعددة اللغات وقابلة للبحث لأرشيف PURSUE UAP

**الموقع:** https://www.ufolens.com  ·  **API:** https://www.ufolens.com/api/v1  ·  **الأرشيف المصدر:** https://www.war.gov/ufo

`ufolens.com` هو مشروع بيعيد نشر أرشيف **PURSUE** الخاص بوزارة الحرب الأمريكية للسجلات السرية لـ UAP / UFO كمنصة معرفية: بحث بالنص الكامل، ترجمة آلية عبر المجموعة كلها، استكشاف الخرائط والخط الزمني، و API عام بصيغة JSON. الوثائق المصدر هي من أعمال حكومة الولايات المتحدة الفيدرالية، وداخل الولايات المتحدة تعتبر ملكية عامة ([17 U.S.C. §105](https://www.copyright.gov/title17/92chap1.html#105)). هذا المشروع **غير تابع لحكومة الولايات المتحدة**، ولا يستخدم أي شعارات رسمية، ولا يعكس أبدًا أي أجزاء محجوبة من النصوص.

### الهيكلية

```
الجهاز المحلي (Apple Silicon، IP سكني)             شبكة الحافة (Edge)
─────────────────────────────────────────           ─────────────────────────
pipeline/  (Python 3.10، نواة stdlib-only)          worker/  (TypeScript, Hono.js)
  fetch → OCR → translate → publish  (مسار أمامي فقط)  /{lang}/...   صفحات
  OCR: محرك مفتوح المصدر (Tesseract CLI كبديل)      /api/v1/...   API عام
  translate / NER: LLM محلي (Gemma عبر Ollama)       /admin        لوحة تحكم المشغل
  state: SQLite manifest                            مدعوم بـ: قاعدة بيانات SQL على الحافة،
        │                                             تخزين كائنات (ملفات PDF المصدر)، ذاكرة تخزين مؤقت KV
        └─ ينشر حزمة: SQL + بيان الأصول + قائمة مسح ذاكرة التخزين المؤقت ─┘
```

- **تكلفة صفرية للذكاء الاصطناعي السحابي لكل وثيقة.** عمليات OCR والترجمة تشتغل محليًا؛ وآلة الحالة ذات المسار الأمامي فقط (`discovered → downloaded → ocr_done → translated → published`) بتضمن عدم إعادة معالجة أي وثيقة إلا إذا تغيرت.
- **نواة خط الأنابيب ما إلها اعتماديات من طرف ثالث** — وحدات التحليل / البيان / التغييرات بتشتغل وبتختبر على Python نظيف بدون أي شي مثبت بـ pip؛ مراحل OCR/الترجمة بتتراجع برشاقة لما تكون الحزم الاختيارية غايبة.
- **موقع الحافة** بيطبق ترويسات أمان صارمة + CSP (ما في `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض لغوي عبر `Accept-Language` + ربط البلدان، ذاكرة تخزين مؤقت للصفحات بـ KV لمدة 30 يومًا، و cron يومي للصيانة.
- **تحديثات تدريجية:** كاشف التغييرات بيقارن فهرس المصدر وبيرجع بس التغييرات لخط الأنابيب.

### للمطورين

الـ API العام على https://www.ufolens.com/api/v1 بيرجع وثائق وبيانات وصفية بصيغة JSON. الوصول المجهول محدود بمعدل معين؛ اطلب مفتاحًا للوصول لمستويات الباحثين/المطورين. شوف قسم الـ API على الموقع لنقاط النهاية والحدود.

### الحالة

الكود مكتمل؛ الموقع منشور على https://www.ufolens.com. قاعدة بيانات الإنتاج بيتم تعبئتها عن طريق تشغيل خط الأنابيب الأوفلاين ونشر الحزمة للأمام (`cli_publish run --remote`). وثائق التصميم الكاملة موجودة في `docs/20260511/`.

### الترخيص / الحدود

- الوثائق المصدر: من أعمال حكومة الولايات المتحدة الفيدرالية، ملكية عامة داخل الولايات المتحدة.
- الكود الخاص بهالمنصة: شوف `LICENSE`.
- الموقع بيرسل `Tdm-Reservation: 1` و `X-Robots-Tag: noai, noimageai` — قابل للفهرسة من قبل محركات البحث، ومستبعد من تدريب/كشط الذكاء الاصطناعي.
- لقطات الفيديو منسوبة لـ DVIDS / AARO ولا يطالب بها هذا المشروع.

نرحب بالـ Issues والـ PRs. الرجاء قراءة `CLAUDE.md` و `docs/20260511/00-*` قبل فتح تغييرات هيكلية.

