# GitHub — المنشور 3 من 3 · ملاحظات حول الهيكلية (مناقشة بأسلوب ADR)

**يُستعمل كـ:** مناقشة تحت "عرض ومشاركة" / "هيكلية"، أو بذرة لـ ADR في `docs/`.
**كلمات مفتاحية:** هيكلية، ADR، آلة حالة ذات اتجاه واحد، LLM محلي، Ollama، OCR، حوسبة الحافة، CSP، ترويسات الأمان، pipeline بيانات، هندسة التكاليف، بيان SQLite، D1، R2، KV
**روابط تشعبية:** https://www.ufolens.com · https://www.ufolens.com/api/v1 · https://www.war.gov/ufo

---

## علاش ufolens.com مبني بالطريقة هاذي

ملاحظات حول القرارات الثلاثة التي شكلت [ufolens.com](https://www.ufolens.com) (إعادة البناء القابلة للبحث والمتعددة اللغات لـ[أرشيف PURSUE UAP](https://www.war.gov/ufo)). نرحب بالتعليقات / الاعتراضات.

### 1. الـ pipeline هو آلة حالة ذات اتجاه واحد — عن قصد

الحالات: `مكتشف ← محمل ← تم_التعرف_الضوئي ← مترجم ← منشور`. تتحرك الوثيقة إلى الأمام فقط، وفقط عندما يكون هناك عمل للقيام به. لا يتم إعادة معالجة المحتوى المنشور أبدًا ما لم يكتشف كاشف الدلتا أن المصدر قد تغير بالفعل.

**علاش:** عمليات الـ OCR والترجمة هي العمليات المكلفة، والأرشيف ينمو بمرور الوقت. أي pipeline "يعيد تشغيل كل شيء ليكون آمنًا" له تكلفة غير محدودة. جعل الانتقالات إلى الوراء مستحيلة يجعل الفاتورة الجامحة مستحيلة. سقف التكلفة هو خاصية من خصائص الرسم البياني للحالة، وليس نتاج يقظة المشغل.

**التكلفة:** هجرات المخطط وإعادة المعالجة عن قصد هي أمور صعبة بشكل متعمد. مقايضة مقبولة.

### 2. الـ OCR والترجمة تعمل على LLM محلي، وليس API سحابي

OCR: محرك مفتوح المصدر، مع Tesseract CLI كبديل. الترجمة + NER: Gemma عبر Ollama، على كمبيوتر محمول Apple Silicon.

**علاش:** تكلفة هامشية صفرية لكل وثيقة؛ قابلية للتكرار (نموذج ثابت + prompts)؛ وخطوة الجلب يجب أن تعمل بالفعل من IP سكني (المصدر محمي بـ Akamai Bot Manager — `curl` يتلقى 403)، لذلك فإن الكمبيوتر المحمول مشارك في الحلقة على أي حال.

**التكلفة:** جودة الترجمة أقل من نموذج متطور. بالنسبة لمجموعة مرجعية حيث الأصل الإنجليزي دائمًا على بعد نقرة واحدة، هذا مقبول. لا ندعي أن الترجمات موثوقة.

### 3. النصفان يتقاسمان واجهة واحدة بالضبط: حزمة منشورة

الـ pipeline لا يكتب أبدًا في قاعدة البيانات الإنتاجية مباشرة. إنه يصدر `{ SQL، بيان الأصول، قائمة تطهير ذاكرة التخزين المؤقت }`. "النشر" = تطبيق تلك الحزمة إلى الأمام (دفع SQL إلى قاعدة بيانات SQL الحافة، مزامنة الأصول مع تخزين الكائنات، تطهير مفاتيح ذاكرة التخزين المؤقت المحددة).

**علاش:** يمكن للجانب المحلي والجانب الحافي التطور بشكل مستقل؛ الحزمة قابلة للمراجعة؛ و "نشر البيانات" له نفس الشكل في كل مرة. الـ Worker هو تطبيق TypeScript/Hono صغير — CSP صارم (بدون `unsafe-inline`؛ JSON-LD المضمن مثبت بـ sha256)، تفاوض `Accept-Language` + بلد→لغة، ذاكرة تخزين مؤقت للصفحات في KV لمدة 30 يومًا، ومهمة صيانة يومية (cron) — ولا يحتاج أبدًا إلى معرفة كيفية صنع البيانات.

**التكلفة:** تغيير مخطط D1 يلمس ملفين (`pipeline/lib/manifest_schema.sql`, `db/schema.sql`). تأمين رخيص.

### أمور غير قابلة للتفاوض مدمجة في السلوك

- غير منتسب للحكومة الأمريكية؛ لا توجد شارات رسمية.
- يتم الحفاظ على عمليات التنقيح في المصدر، ولا يتم عكسها أبدًا.
- الفيديو منسوب إلى DVIDS / AARO.
- `Tdm-Reservation: 1` + `X-Robots-Tag: noai, noimageai` على مستوى الموقع — قابل للفهرسة من قبل محركات البحث، مع إلغاء الاشتراك في الكشط لأغراض الذكاء الاصطناعي.

الموقع المباشر: https://www.ufolens.com · الـ API: https://www.ufolens.com/api/v1

